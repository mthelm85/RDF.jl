var documenterSearchIndex = {"docs":
[{"category":"section","location":"graphs/#Graphs-and-Datasets","page":"Graphs & Datasets","text":"","title":"Graphs & Datasets"},{"category":"section","location":"graphs/#Graph","page":"Graphs & Datasets","text":"A Graph is an in-memory collection of RDF triples backed by a hexastore index — six sorted arrays covering every (s, p, o) permutation. This gives O(log n) pattern matching on any combination of bound and unbound positions without needing a full table scan.","title":"Graph"},{"category":"section","location":"graphs/#Adding-and-removing-triples","page":"Graphs & Datasets","text":"g = Graph()\npush!(g, Triple(ex.alice, rdf.type, ex.Person))\npush!(g, Triple(ex.alice, foaf.name, Literal(\"Alice\")))\ndelete!(g, Triple(ex.alice, rdf.type, ex.Person))\n\nlength(g)    # number of triples\nisempty(g)   # true if the graph has no triples\n\nTriple(ex.alice, foaf.name, Literal(\"Alice\")) in g   # membership test\n\nThe do-block constructor lets you build a graph in one expression:\n\ng = Graph() do g\n    push!(g, Triple(ex.alice, rdf.type, ex.Person))\n    push!(g, Triple(ex.alice, foaf.name, Literal(\"Alice\")))\n    push!(g, Triple(ex.bob, rdf.type, ex.Person))\nend","title":"Adding and removing triples"},{"category":"section","location":"graphs/#Bulk-loading","page":"Graphs & Datasets","text":"# Much faster than push! in a loop for large collections\nbulk_load!(g, triples)","title":"Bulk loading"},{"category":"section","location":"graphs/#Pattern-matching","page":"Graphs & Datasets","text":"match accepts any combination of subject, predicate, and object keyword arguments. Omitting an argument means \"wildcard\".\n\n# All triples (full scan)\nfor t in match(g)\n    println(t)\nend\n\n# All subjects of type Person\nfor t in match(g; predicate=rdf.type, object=ex.Person)\n    println(t.subject)\nend\n\n# All predicates for alice\nfor t in match(g; subject=ex.alice)\n    println(t.predicate, \" => \", t.object)\nend\n\n# Single specific triple (O(log n) lookup)\nresults = collect(match(g; subject=ex.alice, predicate=foaf.name, object=Literal(\"Alice\")))\n\nThe iterator implements the Tables.jl interface so match results can be converted to a DataFrame directly:\n\nusing DataFrames\ndf = DataFrame(match(g; predicate=rdf.type))","title":"Pattern matching"},{"category":"section","location":"graphs/#Projection-helpers","page":"Graphs & Datasets","text":"subjects(g)    # Set{SubjectTerm}  — all distinct subjects\npredicates(g)  # Set{IRI}          — all distinct predicates\nobjects(g)     # Set{ObjectTerm}   — all distinct objects","title":"Projection helpers"},{"category":"section","location":"graphs/#Raw-ID-iteration","page":"Graphs & Datasets","text":"For performance-critical code that needs to iterate large graphs without constructing Triple structs, RDF.jl exposes two zero-allocation alternatives:\n\nBoth eachid and match_ids yield NTuple{3,UInt32} tuples of interned term IDs.  Use resolve_term to convert an ID back to an RDFTerm, and term_id to look up the ID for a term you already have.\n\n# Iterate without allocating Triple structs\nfor (s_id, p_id, o_id) in eachid(g)\n    println(resolve_term(s_id), \" -- \", resolve_term(p_id), \" --> \", resolve_term(o_id))\nend\n\n# Pattern-filtered raw-ID iteration\np_id = term_id(foaf.knows)\nfor (s, p, o) in match_ids(g; predicate=p_id)\n    println(resolve_term(s), \" knows \", resolve_term(o))\nend","title":"Raw-ID iteration"},{"category":"section","location":"graphs/#Concurrency-and-the-term-registry","page":"Graphs & Datasets","text":"RDF.jl interns every distinct term (IRI, literal, blank node) into a global, process-wide registry that maps terms to UInt32 IDs. Two consequences of this design matter for long-running or multi-threaded programs:\n\nThreading contract.\n\nInterning is thread-safe: any number of threads may build graphs concurrently (push!, bulk_load!, parsing).\nReading is thread-safe: any number of threads may query already-built graphs concurrently (match, sparql, iteration, resolve_term).\nResolving an ID you already hold is safe while other threads intern new terms — the ID→term table is append-only, so established IDs stay valid. An ID produced by a concurrent task must be handed over with normal synchronization (@sync, a Channel, a lock) before it is resolved.\nA single Graph/Dataset is not safe for concurrent mutation, and mutating a graph while another thread queries that same graph is a data race — the same contract as Julia's built-in collections.\n\nRegistry lifetime. Interned terms are never freed: every distinct term seen during the process lifetime occupies a registry slot until exit, even after all graphs containing it are garbage-collected. For scripts and notebooks this is irrelevant. For long-running services that continuously ingest new vocabularies (e.g. a pipeline processing unbounded document streams), the registry grows monotonically — budget roughly 100 bytes per distinct term, and restart or shard across worker processes if unbounded distinct-term growth is expected. Repeated use of the same terms costs nothing extra.","title":"Concurrency and the term registry"},{"category":"section","location":"graphs/#Set-operations","page":"Graphs & Datasets","text":"Standard set operations return new graphs without modifying their arguments:\n\ng3 = union(g1, g2)      # triples in either g1 or g2\ng4 = intersect(g1, g2)  # triples in both g1 and g2\ng5 = setdiff(g1, g2)    # triples in g1 but not g2\nissubgraph(g1, g2)       # true if every triple in g1 is also in g2","title":"Set operations"},{"category":"section","location":"graphs/#Merging-and-skolemization","page":"Graphs & Datasets","text":"merge!(g1, g2)    # add all triples from g2 into g1 (in place)\n\n# Replace blank nodes with IRIs scoped to a base\ng_skolem = skolemize(g, \"http://example.org/.well-known/genid/\")","title":"Merging and skolemization"},{"category":"section","location":"graphs/#Graph-isomorphism","page":"Graphs & Datasets","text":"Two graphs are isomorphic if there exists a bijection on blank nodes that makes them identical:\n\nisomorphic(g1, g2)   # true / false\ng1 ≅ g2              # infix alias","title":"Graph isomorphism"},{"category":"section","location":"graphs/#Blank-nodes","page":"Graphs & Datasets","text":"blank_nodes(g)  # Set{BlankNode} — all blank nodes owned by g\n\n","title":"Blank nodes"},{"category":"section","location":"graphs/#Dataset","page":"Graphs & Datasets","text":"A Dataset is an RDF dataset: a default graph plus zero or more named graphs, each identified by an IRI or BlankNode.\n\nds = Dataset()\nds = Dataset(; default_graph=g)\n\n# Named graph access (dict-like)\nds[IRI(\"http://example.org/g1\")] = g1\ng = ds[IRI(\"http://example.org/g1\")]\nhaskey(ds, IRI(\"http://example.org/g1\"))  # true\ndelete!(ds, IRI(\"http://example.org/g1\"))\n\n# Iterate named graphs\nfor (name, graph) in ds\n    println(name, \" has \", length(graph), \" triples\")\nend\n\nlength(ds)  # number of named graphs (not counting the default graph)","title":"Dataset"},{"category":"section","location":"graphs/#Dataset-pattern-matching","page":"Graphs & Datasets","text":"match on a Dataset returns Quad values instead of Triple values. Each Quad has fields subject, predicate, object, and graph.\n\n# Match across all named graphs\nfor q in match(ds; predicate=rdf.type)\n    println(q.subject, \" a \", q.object, \" in graph \", q.graph)\nend\n\n# Restrict to a specific named graph\nfor q in match(ds; graph=IRI(\"http://example.org/g1\"), predicate=rdf.type)\n    println(q.subject)\nend","title":"Dataset pattern matching"},{"category":"section","location":"graphs/#Counting-triples","page":"Graphs & Datasets","text":"ntriples(ds)          # total triples across all graphs (including default)\ncollect(quads(ds))    # lazy iterator of all Quad values including default graph\n\n","title":"Counting triples"},{"category":"section","location":"graphs/#Graphs.jl-integration","page":"Graphs & Datasets","text":"RDF.jl bridges into the Graphs.jl ecosystem, letting you run graph algorithms — PageRank, betweenness centrality, shortest paths, community detection, and more — directly on RDF data.\n\nThree conversion strategies correspond to the Projection, Weighting, and Customisation approaches described in the knowledge-graph analytics literature:","title":"Graphs.jl integration"},{"category":"section","location":"graphs/#Projection-—-to_digraph","page":"Graphs & Datasets","text":"Drop predicate labels and project onto a SimpleDiGraph for topology-based analytics:\n\n# All-predicates reachability graph\nresult  = to_digraph(g)\npr      = pagerank(result.graph)\ntop     = argmax(pr)\nprintln(\"Most central entity: \", resolve_term(result.terms[top]))\n\n# Single-predicate lossless projection\nresult  = to_digraph(g, foaf.knows)\ncc      = weakly_connected_components(result.graph)\nprintln(\"Social network has $(length(cc)) components\")","title":"Projection — to_digraph"},{"category":"section","location":"graphs/#Weighting-—-to_weighted_digraph","page":"Graphs & Datasets","text":"Attach numeric edge weights derived from literal objects:\n\n# Weight edges by a numeric literal property on the object\nresult = to_weighted_digraph(g, schema.distance;\n             weight = obj -> tryparse(Float64, obj.lexical_form) |> something)\nsrc    = vertex_id(result.graph, term_id(ex.A))\nds     = dijkstra_shortest_paths(result.graph, src)","title":"Weighting — to_weighted_digraph"},{"category":"section","location":"graphs/#Customisation-—-RDFDiGraph","page":"Graphs & Datasets","text":"RDFDiGraph is a full AbstractGraph{Int} wrapper: every Graphs.jl algorithm accepts it without any data copy, and predicate information is preserved and queryable per-edge:\n\nrdfdg   = RDFDiGraph(g)\n\n# Standard Graphs.jl algorithms\npr  = pagerank(rdfdg)\nbc  = betweenness_centrality(rdfdg)\nwcc = weakly_connected_components(rdfdg)\n\n# Map results back to RDF terms\nalice_v = vertex_id(rdfdg, ex.alice)\nprintln(\"Alice's PageRank: \", pr[alice_v])\n\n# Recover predicate information on an edge\nbob_v   = vertex_id(rdfdg, ex.bob)\nprintln(\"Alice→Bob via: \", edge_predicates(rdfdg, alice_v, bob_v))\n\n# Resolve a vertex back to an RDFTerm\nprintln(resolve_vertex(rdfdg, argmax(pr)))\n\n","title":"Customisation — RDFDiGraph"},{"category":"section","location":"graphs/#Triples-and-Quads","page":"Graphs & Datasets","text":"A Triple holds subject::SubjectTerm, predicate::IRI, object::ObjectTerm. A Quad extends this with graph::GraphName.\n\nTriplePattern is used internally by SPARQL — it allows variables (nothing) in any position.","title":"Triples and Quads"},{"category":"type","location":"graphs/#RDF.Graph","page":"Graphs & Datasets","text":"Graph()\nGraph(f::Function)\n\nAn RDF graph backed by a hexastore index for O(log n) pattern matching on any combination of subject, predicate, and object.\n\nSupports push!, delete!, in, length, iterate, union, intersect, setdiff, and merge!. The do-block form yields the graph to f and returns it:\n\ng = Graph() do g\n    push!(g, Triple(ex.alice, rdf.type, ex.Person))\n    push!(g, Triple(ex.alice, ex.name, Literal(\"Alice\")))\nend\n\n\n\n\n\n","title":"RDF.Graph"},{"category":"function","location":"graphs/#RDF.bulk_load!","page":"Graphs & Datasets","text":"bulk_load!(g::Graph, triples) -> g\n\nLoad a large collection of triples into g at once using a single sort-and-merge pass — O(n log n) rather than O(n²) for repeated push!. Significantly faster than push! in a loop for thousands or millions of triples.\n\n# From a parsed graph\nbulk_load!(g, read(\"data.nt\", MIME\"application/n-triples\"(), Graph))\n\n# From a generator\nbulk_load!(g, (Triple(ex[string(i)], rdf.type, ex.Thing) for i in 1:100_000))\n\n\n\n\n\n","title":"RDF.bulk_load!"},{"category":"function","location":"graphs/#Base.match","page":"Graphs & Datasets","text":"match(g::Graph; subject=nothing, predicate=nothing, object=nothing)\nmatch(g::Graph, pattern::TriplePattern)\nmatch(ds::Dataset; subject=nothing, predicate=nothing, object=nothing, graph=nothing)\n\nReturn a lazy iterator over all triples (or quads) matching the given pattern. Any omitted keyword argument acts as a wildcard. The hexastore backing the graph automatically selects the most efficient index for the given bound positions.\n\n# All triples with a given predicate\nfor t in match(g; predicate=rdf.type)\n    println(t.subject)\nend\n\n# Exact triple lookup\nmatch(g; subject=ex.alice, predicate=rdf.type, object=ex.Person)\n\n# Dataset: restrict to a named graph\nmatch(ds; predicate=rdf.type, graph=IRI(\"http://example.org/g1\"))\n\n# Dataset: default graph only\nmatch(ds; graph=:default)\n\n\n\n\n\nmatch(rg::RemoteGraph; subject=nothing, predicate=nothing, object=nothing)\n    -> Vector{Triple}\n\nPattern matching against a remote endpoint.  Unbound positions are wildcards. Translates to a SELECT query (or ASK when all three positions are bound) and materialises the matching triples.\n\n\n\n\n\n","title":"Base.match"},{"category":"function","location":"graphs/#RDF.subjects","page":"Graphs & Datasets","text":"subjects(g::Graph; kwargs...) -> iterator\n\nLazy iterator over the subjects of all triples matching kwargs. Equivalent to (t.subject for t in match(g; kwargs...)).\n\n\n\n\n\n","title":"RDF.subjects"},{"category":"function","location":"graphs/#RDF.predicates","page":"Graphs & Datasets","text":"predicates(g::Graph; kwargs...) -> iterator\n\nLazy iterator over the predicates of all triples matching kwargs.\n\n\n\n\n\n","title":"RDF.predicates"},{"category":"function","location":"graphs/#RDF.objects","page":"Graphs & Datasets","text":"objects(g::Graph; kwargs...) -> iterator\n\nLazy iterator over the objects of all triples matching kwargs.\n\n\n\n\n\n","title":"RDF.objects"},{"category":"function","location":"graphs/#RDF.eachid","page":"Graphs & Datasets","text":"eachid(g::Graph) -> iterator\n\nZero-allocation iterator over every triple in g as a raw (s_id, p_id, o_id)::NTuple{3,UInt32} tuple, reading directly from the underlying hexastore without constructing Triple structs or boxing union-typed terms.\n\nUse resolve_term(id) to convert an ID back to an RDFTerm when needed. This is the preferred inner loop for bulk write operations and any code that only needs a subset of the resolved terms per row.\n\nfor (s_id, p_id, o_id) in eachid(g)\n    println(resolve_term(s_id), \" -- \", resolve_term(p_id), \" --> \", resolve_term(o_id))\nend\n\nSee also: match_ids for pattern-filtered raw-ID iteration.\n\n\n\n\n\n","title":"RDF.eachid"},{"category":"function","location":"graphs/#RDF.match_ids","page":"Graphs & Datasets","text":"match_ids(g::Graph; subject=nothing, predicate=nothing, object=nothing)\n\nLike match but yields raw (s_id, p_id, o_id)::NTuple{3,UInt32} tuples rather than resolved Triples.  Arguments are UInt32 IDs (use term_id(term) to look up the ID for a term) or nothing for wildcards. Pass UInt32(0) for any bound position that is definitely not in the registry to get an immediately-empty iterator.\n\nThis is a zero-allocation alternative to match for code that needs pattern filtering without the overhead of _resolve() and Triple construction.\n\n\n\n\n\n","title":"RDF.match_ids"},{"category":"function","location":"graphs/#RDF.resolve_term","page":"Graphs & Datasets","text":"resolve_term(id::UInt32) -> RDFTerm\n\nConvert an interned term ID (as returned by eachid, match_ids, term_id, or result.terms[v] from to_digraph / to_weighted_digraph) back to its RDFTerm.\n\nfor (s_id, p_id, o_id) in eachid(g)\n    println(resolve_term(s_id), \" -- \", resolve_term(p_id), \" --> \", resolve_term(o_id))\nend\n\nresult = to_digraph(g, foaf.knows)\ntop    = argmax(pagerank(result.graph))\nprintln(\"Most central: \", resolve_term(result.terms[top]))\n\n\n\n\n\n","title":"RDF.resolve_term"},{"category":"function","location":"graphs/#RDF.term_id","page":"Graphs & Datasets","text":"term_id(term::RDFTerm) -> UInt32\n\nReturn the interned ID for term, or UInt32(0) if the term has never been interned (i.e., it does not exist in any graph currently loaded in memory).\n\nThis is the read-only counterpart to the internal _intern! function.  Use it to obtain an ID for passing to match_ids:\n\np_id = term_id(foaf.knows)            # UInt32(0) if foaf.knows not in any graph\nfor (s, p, o) in match_ids(g; predicate=p_id)\n    println(resolve_term(s))\nend\n\nReturns UInt32(0) — which match_ids treats as \"not found\" — rather than throwing, so it is always safe to call on unknown terms.\n\n\n\n\n\nterm_id(rdfdg::RDFDiGraph, v::Int) -> UInt32\n\nReturn the interned term ID for vertex v.  Use resolve_term(term_id(rdfdg, v)) to recover the RDFTerm.\n\n\n\n\n\n","title":"RDF.term_id"},{"category":"function","location":"graphs/#Base.merge!","page":"Graphs & Datasets","text":"merge!(g1::Graph, g2::Graph) -> g1\n\nMerge all triples from g2 into g1 in-place. Blank nodes from g2 that collide with blank nodes already in g1 are renamed to fresh identifiers.\n\n\n\n\n\n","title":"Base.merge!"},{"category":"function","location":"graphs/#RDF.issubgraph","page":"Graphs & Datasets","text":"issubgraph(g1::Graph, g2::Graph) -> Bool\n\nReturn true if every triple in g1 also appears in g2.\n\n\n\n\n\n","title":"RDF.issubgraph"},{"category":"function","location":"graphs/#RDF.skolemize","page":"Graphs & Datasets","text":"skolemize(g::Graph; base::String) -> Graph\n\nReturn a new graph with all blank nodes replaced by IRIs of the form base * string(id). The base string should end with / or #.\n\nskolemize(g; base=\"http://example.org/.well-known/genid/\")\n\n\n\n\n\n","title":"RDF.skolemize"},{"category":"function","location":"graphs/#RDF.deskolemize","page":"Graphs & Datasets","text":"deskolemize(g::Graph; base::String) -> Graph\n\nReverse of skolemize: replace IRIs whose value starts with base with fresh blank nodes.\n\n\n\n\n\n","title":"RDF.deskolemize"},{"category":"function","location":"graphs/#RDF.isomorphic","page":"Graphs & Datasets","text":"isomorphic(g1::Graph, g2::Graph) -> Bool\nisomorphic(ds1::Dataset, ds2::Dataset) -> Bool\ng1 ≅ g2\n\nReturn true if g1 and g2 are RDF-isomorphic: there exists a bijection on blank node identifiers such that mapping g1's blank nodes produces exactly the same set of triples as g2.\n\n\n\n\n\n","title":"RDF.isomorphic"},{"category":"function","location":"graphs/#RDF.blank_nodes","page":"Graphs & Datasets","text":"blank_nodes(g::Graph) -> Set{BlankNode}\n\nReturn the set of blank nodes owned by g.\n\n\n\n\n\n","title":"RDF.blank_nodes"},{"category":"type","location":"graphs/#RDF.Dataset","page":"Graphs & Datasets","text":"Dataset(; default_graph::Graph = Graph())\n\nAn RDF dataset consisting of a default graph and zero or more named graphs. Named graphs are accessed with dict-like syntax:\n\nds = Dataset()\nds[IRI(\"http://example.org/g1\")] = g\ng2 = ds[IRI(\"http://example.org/g1\")]\nhaskey(ds, IRI(\"http://example.org/g1\"))  # true\n\nIterating a Dataset yields (name::GraphName, graph::Graph) pairs for the named graphs only. Use quads to iterate all triples including the default graph.\n\n\n\n\n\n","title":"RDF.Dataset"},{"category":"function","location":"graphs/#RDF.ntriples","page":"Graphs & Datasets","text":"ntriples(ds::Dataset) -> Int\n\nReturn the total number of triples across the default graph and all named graphs.\n\n\n\n\n\n","title":"RDF.ntriples"},{"category":"function","location":"graphs/#RDF.quads","page":"Graphs & Datasets","text":"quads(ds::Dataset)\n\nReturn a lazy iterator over all Quads in the dataset, including the default graph (quad.graph === nothing) and all named graphs.\n\n\n\n\n\n","title":"RDF.quads"},{"category":"function","location":"graphs/#RDF.to_digraph","page":"Graphs & Datasets","text":"to_digraph(g::Graph) -> (graph::SimpleDiGraph, terms::Vector{UInt32})\n\nProject every triple in g onto a SimpleDiGraph.  Each unique subject or object term becomes a vertex; each unique (subject, object) pair becomes a directed edge.  Predicate labels are dropped (textbook Projection strategy). Multiple predicates between the same two terms collapse to a single edge.\n\nReturns a named tuple with:\n\ngraph  — the Graphs.SimpleDiGraph\nterms  — Vector{UInt32} mapping vertex index → interned term ID. Use resolve_term(terms[v]) to recover the RDFTerm for vertex v.\n\nresult = to_digraph(g)\npr = pagerank(result.graph)   # PageRank over all entities\ntop = sortperm(pr; rev=true)\nprintln(\"Most central: \", resolve_term(result.terms[top[1]]))\n\nSee also: to_digraph(g, pred), to_weighted_digraph, RDFDiGraph.\n\n\n\n\n\nto_digraph(g::Graph, pred::IRI) -> (graph::SimpleDiGraph, terms::Vector{UInt32})\n\nProject the subset of triples in g whose predicate is pred onto a SimpleDiGraph.  Only terms that appear as subjects or objects of a pred triple are included as vertices.  This is a lossless projection for the connectivity of that single edge type — the resulting graph records exactly the same reachability as the original RDF triples with that predicate.\n\nReturns the same named tuple as to_digraph(g).\n\n# Social-network analysis using foaf:knows connectivity\nresult  = to_digraph(g, foaf.knows)\npr      = pagerank(result.graph)\ntop     = argmax(pr)\nprintln(\"Most central person: \", resolve_term(result.terms[top]))\n\n\n\n\n\n","title":"RDF.to_digraph"},{"category":"function","location":"graphs/#RDF.to_weighted_digraph","page":"Graphs & Datasets","text":"to_weighted_digraph(g::Graph, pred::IRI; weight::Function = _ -> 1.0)\n    -> (graph::SimpleWeightedDiGraph, terms::Vector{UInt32})\n\nProject the subset of g's triples with predicate pred onto a SimpleWeightedDiGraph.  Each edge's weight is computed by calling weight(object_term) on the triple's object.\n\nThe weight function receives the object RDFTerm and must return a Float64.  For graphs where objects are numeric literals, a common choice is:\n\nweight = obj -> begin\n    obj isa Literal || return 1.0\n    v = tryparse(Float64, obj.lexical_form)\n    v === nothing ? 1.0 : v\nend\n\nReturns a named tuple with:\n\ngraph  — a SimpleWeightedDiGraph{Int,Float64}\nterms  — Vector{UInt32} mapping vertex index → interned term ID\n\n# Dijkstra's shortest path weighted by a numeric literal property\nresult = to_weighted_digraph(g, ex.travelTime; weight = obj -> tryvalue(obj, Float64, Inf))\nsrc    = vertex_id_from_terms(result.terms, ex.A)\nds     = dijkstra_shortest_paths(result.graph, src)\n\nSee also: to_digraph, RDFDiGraph.\n\n\n\n\n\n","title":"RDF.to_weighted_digraph"},{"category":"type","location":"graphs/#RDF.RDFDiGraph","page":"Graphs & Datasets","text":"RDFDiGraph(g::Graph) <: Graphs.AbstractGraph{Int}\n\nA zero-copy AbstractGraph wrapper around an RDF Graph.  Every unique subject or object term becomes an integer vertex; every unique (subject, object) pair becomes a directed edge.\n\nPredicate information is preserved: edge_predicates(rdfdg, u, v) returns all predicates on a given edge.  Term ↔ vertex mapping is provided by vertex_id and resolve_vertex.\n\nThe full Graphs.jl algorithm suite — pagerank, betweenness_centrality, connected_components, dijkstra_shortest_paths, BFS/DFS, etc. — works directly on RDFDiGraph without any additional transformation.\n\nrdfdg   = RDFDiGraph(g)\npr      = pagerank(rdfdg)\nalice_v = vertex_id(rdfdg, ex.alice)\nprintln(\"Alice's PageRank: \", pr[alice_v])\nprintln(\"Predicates on alice→bob: \", edge_predicates(rdfdg, alice_v, vertex_id(rdfdg, ex.bob)))\n\nSee also: to_digraph, to_weighted_digraph, vertex_id, term_id, resolve_vertex, edge_predicates.\n\n\n\n\n\n","title":"RDF.RDFDiGraph"},{"category":"function","location":"graphs/#RDF.vertex_id","page":"Graphs & Datasets","text":"vertex_id(rdfdg::RDFDiGraph, term::RDFTerm) -> Int\n\nReturn the vertex index for term in rdfdg, or 0 if the term is not a vertex (i.e., it does not appear as a subject or object in the underlying RDF graph).\n\n\n\n\n\n","title":"RDF.vertex_id"},{"category":"function","location":"graphs/#RDF.resolve_vertex","page":"Graphs & Datasets","text":"resolve_vertex(rdfdg::RDFDiGraph, v::Int) -> RDFTerm\n\nReturn the RDFTerm for vertex v.\n\n\n\n\n\n","title":"RDF.resolve_vertex"},{"category":"function","location":"graphs/#RDF.edge_predicates","page":"Graphs & Datasets","text":"edge_predicates(rdfdg::RDFDiGraph, u::Int, v::Int) -> Vector{IRI}\n\nReturn every predicate IRI on the directed edge from vertex u to vertex v. Returns an empty vector if no such edge exists.\n\nThis is the mechanism for recovering predicate information that the topology of RDFDiGraph (a simple directed graph) does not encode directly.\n\n\n\n\n\n","title":"RDF.edge_predicates"},{"category":"type","location":"graphs/#RDF.Triple","page":"Graphs & Datasets","text":"Triple(subject, predicate, object)\n\nAn RDF triple. subject must be a SubjectTerm (IRI, BlankNode, or TripleTerm), predicate must be an IRI, and object may be any ObjectTerm.\n\nex = Namespace(\"http://example.org/\")\nTriple(ex.alice, rdf.type, ex.Person)\nTriple(ex.alice, ex.age, Literal(30))\n\n\n\n\n\n","title":"RDF.Triple"},{"category":"type","location":"graphs/#RDF.Quad","page":"Graphs & Datasets","text":"Quad(subject, predicate, object, graph)\nQuad(triple; graph=nothing)\nQuad(triple, graph)\n\nAn RDF quad (triple plus a named graph). graph is a GraphName (IRI or BlankNode) or nothing for the default graph.\n\n\n\n\n\n","title":"RDF.Quad"},{"category":"type","location":"graphs/#RDF.GeneralizedTriple","page":"Graphs & Datasets","text":"GeneralizedTriple(subject, predicate, object)\n\nA generalized RDF triple where any position may hold any RDFTerm, including literals in subject/predicate position. Used for non-standard graph formats.\n\n\n\n\n\n","title":"RDF.GeneralizedTriple"},{"category":"type","location":"graphs/#RDF.TriplePattern","page":"Graphs & Datasets","text":"TriplePattern(; subject=nothing, predicate=nothing, object=nothing)\n\nA triple pattern for use with match. nothing in any position acts as a wildcard that matches any term.\n\n\n\n\n\n","title":"RDF.TriplePattern"},{"category":"section","location":"serialization/#Serialization","page":"Serialization","text":"RDF.jl uses Julia's standard Base.read / Base.write API with MIME types to dispatch between formats. All parsers and serializers work with IO streams; rdf_read and rdf_write add file-extension–based dispatch.","title":"Serialization"},{"category":"section","location":"serialization/#Supported-formats","page":"Serialization","text":"Format MIME type Extension Read Write\nN-Triples 1.2 application/n-triples .nt ✅ ✅\nN-Quads 1.2 application/n-quads .nq ✅ ✅\nTurtle 1.2 text/turtle .ttl ✅ ✅\nJSON-LD 1.1 application/ld+json .jsonld ✅ ✅\n\nThe N-Triples, N-Quads, and Turtle parsers support RDF 1.2 / RDF-star: triple terms <<( s p o )>>, reified triples << s p o >> with optional reifiers (~), annotation blocks ({| … |}, Turtle), directional language tags (\"x\"@en--ltr), and the @version / VERSION directive. All RDF 1.2 W3C N-Triples, N-Quads, and Turtle test suites pass.","title":"Supported formats"},{"category":"section","location":"serialization/#N-Triples","page":"Serialization","text":"# Write a Graph to N-Triples\nwrite(io, MIME\"application/n-triples\"(), g)\n\n# Read a Graph from N-Triples\ng = read(io, MIME\"application/n-triples\"(), Graph)\n\n# Streaming parse (low allocation)\nparse_triples(io, MIME\"application/n-triples\"()) do triple\n    process(triple)\nend","title":"N-Triples"},{"category":"section","location":"serialization/#N-Quads","page":"Serialization","text":"# Write a Dataset to N-Quads\nwrite(io, MIME\"application/n-quads\"(), ds)\n\n# Read a Dataset from N-Quads\nds = read(io, MIME\"application/n-quads\"(), Dataset)","title":"N-Quads"},{"category":"section","location":"serialization/#Turtle","page":"Serialization","text":"# Write a Graph to Turtle (with optional namespace prefixes)\nwrite(io, MIME\"text/turtle\"(), g)\n\n# Read a Graph from Turtle\ng = read(io, MIME\"text/turtle\"(), Graph)\n\n# Read with an explicit base IRI for resolving relative IRIs\ng = read(io, MIME\"text/turtle\"(), Graph, \"http://example.org/base/\")\n\n# File path overload\ng = read(\"data.ttl\", MIME\"text/turtle\"(), Graph)\n\nTurtle output groups triples by subject and uses predicate lists with ; for compact output. Common namespaces are abbreviated automatically.","title":"Turtle"},{"category":"section","location":"serialization/#JSON-LD","page":"Serialization","text":"JSON-LD is the dominant format for linked data on the web. It uses JSON with a @context to map compact terms to full IRIs.\n\n# Read a Graph from JSON-LD\ng = read(io, MIME\"application/ld+json\"(), Graph)\n\n# Read a Dataset (for documents with @graph / named graphs)\nds = read(io, MIME\"application/ld+json\"(), Dataset)\n\n# Write a Graph to JSON-LD (expanded form)\nwrite(io, MIME\"application/ld+json\"(), g)\n\n# Write with a context object for compact output\nwrite(io, MIME\"application/ld+json\"(), g; context=Dict(\n    \"@vocab\" => \"http://schema.org/\",\n    \"name\"   => \"http://schema.org/name\",\n))","title":"JSON-LD"},{"category":"section","location":"serialization/#JSON-LD-example","page":"Serialization","text":"jsonld = \"\"\"\n{\n  \"@context\": {\n    \"ex\":   \"http://example.org/\",\n    \"foaf\": \"http://xmlns.com/foaf/0.1/\",\n    \"name\": \"foaf:name\",\n    \"age\":  {\"@id\": \"foaf:age\", \"@type\": \"xsd:integer\"},\n    \"xsd\":  \"http://www.w3.org/2001/XMLSchema#\"\n  },\n  \"@id\": \"ex:alice\",\n  \"@type\": \"ex:Person\",\n  \"name\": \"Alice\",\n  \"age\": 30,\n  \"foaf:knows\": {\"@id\": \"ex:bob\"}\n}\n\"\"\"\n\ng = read(IOBuffer(jsonld), MIME\"application/ld+json\"(), Graph)\n# => Graph with triples:\n#   ex:alice rdf:type ex:Person\n#   ex:alice foaf:name \"Alice\"\n#   ex:alice foaf:age \"30\"^^xsd:integer\n#   ex:alice foaf:knows ex:bob\n\nnote: Remote contexts\nRemote context loading (@context: \"https://...\") is not supported. Only inline contexts are processed. For remote contexts, fetch them manually and merge into the inline context.","title":"JSON-LD example"},{"category":"section","location":"serialization/#File-extension-dispatch","page":"Serialization","text":"rdf_read and rdf_write choose the format based on the file extension:\n\ng  = rdf_read(\"data.nt\")    # N-Triples\ng  = rdf_read(\"data.ttl\")   # Turtle\nds = rdf_read(\"data.nq\")    # N-Quads → Dataset\ng  = rdf_read(\"data.jsonld\") # JSON-LD\n\nrdf_write(\"out.nt\", g)\nrdf_write(\"out.ttl\", g)\nrdf_write(\"out.nq\", ds)\nrdf_write(\"out.jsonld\", g)","title":"File-extension dispatch"},{"category":"section","location":"serialization/#Streaming-parse","page":"Serialization","text":"For very large files, use the callback form of parse_triples to avoid materializing the entire graph in memory:\n\n# Count triples without building a Graph\nn = Ref(0)\nparse_triples(io, MIME\"application/n-triples\"()) do t\n    n[] += 1\nend\nprintln(n[])  # number of triples","title":"Streaming parse"},{"category":"function","location":"serialization/#RDF.rdf_read","page":"Serialization","text":"rdf_read(path::AbstractString) → Graph | Dataset\n\nRead an RDF file from path, choosing the parser by file extension:\n\nExtension Format Return type\n.nt N-Triples Graph\n.ttl Turtle 1.1 Graph\n.nq N-Quads Dataset\n.jsonld, .json-ld JSON-LD 1.1 Dataset\n\nFor fine-grained control (streaming, base IRI, etc.) use Base.read(io, mime, T) directly.\n\n\n\n\n\n","title":"RDF.rdf_read"},{"category":"function","location":"serialization/#RDF.rdf_write","page":"Serialization","text":"rdf_write(path::AbstractString, g::Graph)\nrdf_write(path::AbstractString, ds::Dataset)\n\nWrite g or ds to path, choosing the serializer by file extension:\n\nExtension Format\n.ttl Turtle 1.1\n.jsonld, .json-ld JSON-LD 1.1\nanything else N-Triples (Graph) or N-Quads (Dataset)\n\n\n\n\n\n","title":"RDF.rdf_write"},{"category":"function","location":"serialization/#RDF.parse_triples","page":"Serialization","text":"parse_triples(f, io, mime)\nparse_triples(io, mime) → iterator\n\nStreaming triple parser. Calls f(triple) for each parsed triple without materializing a Graph, minimising allocations for large files.\n\n# Callback form — processes each triple immediately\nparse_triples(io, MIME\"application/n-triples\"()) do t\n    println(t.subject)\nend\n\n# Iterator form — lazy, pull-based\nfor t in parse_triples(io, MIME\"application/n-triples\"())\n    process(t)\nend\n\nCurrently supported MIME types: application/n-triples.\n\n\n\n\n\n","title":"RDF.parse_triples"},{"category":"section","location":"vocabulary/#Vocabulary-API","page":"Vocabulary API","text":"RDF.jl ships built-in constants for the core W3C namespaces (rdf, rdfs, xsd, owl, skos, dc, dcterms, foaf, schema).  For everything else — domain-specific ontologies, frequently-updated vocabularies, or any namespace you want to introspect — Vocabulary and load_vocabulary provide a unified API.\n\n","title":"Vocabulary API"},{"category":"section","location":"vocabulary/#Quick-start","page":"Vocabulary API","text":"using RDF, HTTP\n\n# Load CTDL (Credential Transparency Description Language)\nctdl = load_vocabulary(\"https://credreg.net/ctdl/schema/encoding/turtle\";\n                        base=\"http://purl.org/ctdl/terms/\")\n\n# Term access — dot notation\nctdl.BachelorDegree    # => IRI(\"http://purl.org/ctdl/terms/BachelorDegree\")\nctdl.Course            # => IRI(\"http://purl.org/ctdl/terms/Course\")\nctdl.estimatedCost     # => IRI(\"http://purl.org/ctdl/terms/estimatedCost\")\n\n# Metadata\nlabel(ctdl, ctdl.Course)     # => \"Course\"\ncomment(ctdl, ctdl.Course)   # => \"Single structured sequence...\"\n\n# Enumerate all indexed terms\nfor iri in terms(ctdl)\n    lbl = label(ctdl, iri)\n    println(lbl !== nothing ? lbl : iri)\nend\n\n","title":"Quick start"},{"category":"section","location":"vocabulary/#Construction","page":"Vocabulary API","text":"","title":"Construction"},{"category":"section","location":"vocabulary/#From-a-file-or-URL","page":"Vocabulary API","text":"# Turtle file (extension auto-detected)\ngo = load_vocabulary(\"go.ttl\"; base=\"http://purl.obolibrary.org/obo/\")\n\n# N-Triples\ndc = load_vocabulary(\"dublin_core.nt\")\n\n# JSON-LD (all named graphs are merged)\nschema_vocab = load_vocabulary(\"schemaorg.jsonld\";\n                                base=\"https://schema.org/\")\n\n# Remote URL — requires HTTP.jl\nusing HTTP\nctdl = load_vocabulary(\"https://credreg.net/ctdl/schema/encoding/turtle\";\n                        base=\"http://purl.org/ctdl/terms/\")\n\n# Explicit format override\nv = load_vocabulary(\"vocab_data\"; format=MIME\"text/turtle\"())","title":"From a file or URL"},{"category":"section","location":"vocabulary/#From-an-existing-Graph","page":"Vocabulary API","text":"If you already have a Graph (from rdf_read, SPARQL CONSTRUCT, inference, etc.) you can wrap it directly:\n\ng = rdf_read(\"my_ontology.ttl\")\nv = Vocabulary(g; base=\"http://example.org/onto/\")\n\n","title":"From an existing Graph"},{"category":"section","location":"vocabulary/#The-base-keyword","page":"Vocabulary API","text":"base serves two roles:\n\nFilter — only IRIs starting with base are indexed.\nFallback — vocab.TermName constructs IRI(base * \"TermName\") even for terms not present as subjects in the graph (e.g. terms added after the vocabulary snapshot was taken).\n\nWithout base, every subject IRI is indexed by its local name (the fragment after the last # or /).  This works well for single-namespace vocabularies; for multi-namespace graphs, local name collisions are silently resolved by keeping the first occurrence — use base or the vocab[\"http://...\"] passthrough form instead.\n\n# With base — scoped to one namespace, with fallback construction\nctdl = load_vocabulary(\"ctdl.ttl\"; base=\"http://purl.org/ctdl/terms/\")\nctdl.NewTerm   # => IRI(\"http://purl.org/ctdl/terms/NewTerm\") even if absent\n\n# Without base — all subject IRIs indexed by local name\nv = load_vocabulary(\"mixed.ttl\")\nv[\"http://a.org/Foo\"]   # => IRI(\"http://a.org/Foo\")  (full IRI passthrough)\n\n","title":"The base keyword"},{"category":"section","location":"vocabulary/#Term-access","page":"Vocabulary API","text":"Syntax Behaviour\nvocab.TermName Local-name lookup; falls back to base * \"TermName\" if base is set; raises KeyError otherwise\nvocab[\"TermName\"] Same as dot notation\nvocab[\"http://full/iri\"] Full IRI passthrough — no lookup, just wraps in IRI(...)\n\n# All three are equivalent when the term exists\nctdl.BachelorDegree\nctdl[\"BachelorDegree\"]\nctdl[\"http://purl.org/ctdl/terms/BachelorDegree\"]\n\n","title":"Term access"},{"category":"section","location":"vocabulary/#Using-vocabulary-terms-in-queries","page":"Vocabulary API","text":"Vocabulary terms are plain IRI values, so they work anywhere an IRI is expected — including SPARQL queries via string interpolation:\n\nresult = sparql(ds, \"\"\"\n  PREFIX ctdl: <http://purl.org/ctdl/terms/>\n  SELECT ?cred ?name WHERE {\n    ?cred a        <$(ctdl.BachelorDegree)> ;\n          ctdl:name ?name .\n  }\n\"\"\")\n\nOr build the triple pattern directly:\n\nfor t in match(g; predicate=rdf.type, object=ctdl.BachelorDegree)\n    println(t.subject)\nend\n\n","title":"Using vocabulary terms in queries"},{"category":"section","location":"vocabulary/#Scientific-domain-vocabularies","page":"Vocabulary API","text":"The Vocabulary API is designed for the rich ecosystem of scientific ontologies:\n\nDomain Vocabulary URL\nBiology Gene Ontology (GO) http://purl.obolibrary.org/obo/go.owl\nChemistry ChEBI http://purl.obolibrary.org/obo/chebi.owl\nEcology Darwin Core https://dwc.tdwg.org/rdf/dwcterms.rdf\nGenomics Sequence Ontology http://purl.obolibrary.org/obo/so.owl\nPhenotypes Human Phenotype Ontology http://purl.obolibrary.org/obo/hp.owl\nProvenance PROV-O http://www.w3.org/ns/prov-o\nCredentials CTDL https://credreg.net/ctdl/schema/encoding/turtle\n\nusing RDF, HTTP\n\n# Darwin Core\ndwc = load_vocabulary(\"https://dwc.tdwg.org/rdf/dwcterms.rdf\";\n                       base=\"http://rs.tdwg.org/dwc/terms/\")\n\ndwc.scientificName    # => IRI(\"http://rs.tdwg.org/dwc/terms/scientificName\")\ndwc.decimalLatitude   # => IRI(\"http://rs.tdwg.org/dwc/terms/decimalLatitude\")\n\nnote: Large ontologies\nOBO ontologies like GO and ChEBI contain tens of thousands of terms and can take a few seconds to parse and index.  Consider caching the resulting Vocabulary object for reuse within a session.","title":"Scientific domain vocabularies"},{"category":"type","location":"vocabulary/#RDF.Vocabulary","page":"Vocabulary API","text":"Vocabulary\n\nAn RDF vocabulary loaded from a graph, file, or URI.  Provides:\n\nDot-notation access to terms: vocab.BachelorDegree\nString indexing: vocab[\"BachelorDegree\"] or vocab[\"http://...\"]\nMetadata: label and comment via rdfs:label / rdfs:comment\nTerm enumeration: terms\n\nConstruct via Vocabulary(graph) or load_vocabulary.\n\n\n\n\n\n","title":"RDF.Vocabulary"},{"category":"function","location":"vocabulary/#RDF.load_vocabulary","page":"Vocabulary API","text":"load_vocabulary(source; base=nothing, format=nothing) -> Vocabulary\n\nLoad an RDF vocabulary from a local file path or a remote HTTP/HTTPS URL.\n\nLocal files — format inferred from extension:\n\nExtension Format\n.ttl Turtle 1.1\n.nt N-Triples\n.nq N-Quads\n.jsonld, .json-ld, .json JSON-LD 1.1\n\nRemote URLs — requires HTTP.jl to be loaded alongside RDF.jl. Content-negotiation requests Turtle first, then N-Triples, then JSON-LD.\n\nbase restricts indexing to IRIs that start with the given string prefix and sets the namespace used by vocab.Term property access.  If omitted, all subject IRIs are indexed by local name (fragment after the last # or /).\n\nformat overrides extension-based detection, e.g. MIME\"text/turtle\"().\n\nExamples\n\n# Local Turtle file\nctdl = load_vocabulary(\"ctdl.ttl\"; base=\"http://purl.org/ctdl/terms/\")\n\n# Remote URL (requires HTTP.jl)\nusing HTTP\nctdl = load_vocabulary(\"https://credreg.net/ctdl/schema/encoding/turtle\";\n                        base=\"http://purl.org/ctdl/terms/\")\n\n# Term access\nctdl.BachelorDegree               # => IRI(\"http://purl.org/ctdl/terms/BachelorDegree\")\nlabel(ctdl, ctdl.Course)          # => \"Course\"\ncomment(ctdl, ctdl.estimatedCost) # => description string\n\n# Enumerate all indexed terms\nfor iri in terms(ctdl)\n    println(label(ctdl, iri), \"  =>  \", iri)\nend\n\n# Use in a SPARQL query\nsparql(ds, \"\"\"\n  SELECT ?cred WHERE {\n    ?cred a <$(ctdl.BachelorDegree)> .\n  }\n\"\"\")\n\n\n\n\n\n","title":"RDF.load_vocabulary"},{"category":"function","location":"vocabulary/#RDF.terms","page":"Vocabulary API","text":"terms(v::Vocabulary)\n\nReturn an iterator over every IRI indexed in the vocabulary.\n\nfor iri in terms(ctdl)\n    println(label(ctdl, iri), \"  =>  \", iri)\nend\n\n\n\n\n\n","title":"RDF.terms"},{"category":"function","location":"vocabulary/#RDF.label","page":"Vocabulary API","text":"label(v::Vocabulary, iri::IRI; lang=\"en\") -> Union{String, Nothing}\n\nReturn the rdfs:label of iri in the vocabulary.\n\nPrefers a label whose language tag starts with lang (default \"en\"), so \"en-US\" matches lang=\"en\".  Falls back to any plain (untagged) literal if no language-matched label exists.  Returns nothing if no label is found.\n\nlabel(ctdl, ctdl.BachelorDegree)             # => \"Bachelor's Degree\"\nlabel(ctdl, ctdl.BachelorDegree; lang=\"es\")  # => Spanish label if present\n\n\n\n\n\n","title":"RDF.label"},{"category":"function","location":"vocabulary/#RDF.comment","page":"Vocabulary API","text":"comment(v::Vocabulary, iri::IRI; lang=\"en\") -> Union{String, Nothing}\n\nReturn the rdfs:comment of iri in the vocabulary.\n\nSame language preference logic as label.\n\ncomment(ctdl, ctdl.Course)\n# => \"Single structured sequence of one or more educational activities\"\n\n\n\n\n\n","title":"RDF.comment"},{"category":"section","location":"ai/#AI-and-GraphRAG","page":"AI & GraphRAG","text":"RDF.jl provides first-class primitives for the modern knowledge-graph + LLM loop: store LLM-extracted facts with their extraction metadata, and ground LLM prompts in focused, token-budgeted subgraphs (GraphRAG).","title":"AI & GraphRAG"},{"category":"section","location":"ai/#Storing-extracted-knowledge-—-RDF-star-annotations","page":"AI & GraphRAG","text":"When an LLM extracts triples from text, the facts are only half the story — you also need the confidence score, the source document, the model that produced them.  RDF.jl attaches this metadata to individual triples using the standard RDF 1.2 reification pattern, which serializes losslessly to every RDF 1.2 format (this is a capability rdflib does not have):\n\nusing RDF\nex = Namespace(\"http://example.org/\")\n\ng = Graph()\n\n# An extraction pipeline storing a fact with its metadata\nt = Triple(ex.alice, ex.employer, ex.acme)\nannotate!(g, t; confidence = 0.92,\n                source     = ex.doc42,\n                model      = Literal(\"claude-fable-5\"),\n                extracted  = Literal(Dates.now()))\n\n# Reading it back\nannotations(g, t)                    # all (predicate => object) pairs\nannotations(g, t, anno.confidence)   # [Literal(0.92)]\n\n# The guardrail filter: keep only high-confidence facts\ntrusted = Graph()\nfor fact in match(g; predicate=ex.employer)\n    confs = annotations(g, fact, anno.confidence)\n    any(value(Float64, c) >= 0.9 for c in confs) && push!(trusted, fact)\nend\n\nUnder the hood annotate! asserts the triple and creates a reifier blank node per statement (_:r rdf:reifies <<( s p o )>>), hanging the annotations off the reifier.  Repeated annotate! calls on the same triple reuse its reifier; multiple reifiers (e.g. two extraction runs annotating the same fact) are aggregated by annotations.","title":"Storing extracted knowledge — RDF-star annotations"},{"category":"section","location":"ai/#Grounding-prompts-—-subgraph-extraction","page":"AI & GraphRAG","text":"The core GraphRAG retrieval step: resolve a query to seed entities, pull a focused subgraph around them, and render it into the prompt.","title":"Grounding prompts — subgraph extraction"},{"category":"section","location":"ai/#The-pipeline","page":"AI & GraphRAG","text":"# 1. Seeds — from entity linking, a vector search, or user input\nseeds = [ex.julia, ex.bezanson]\n\n# 2. Expand: the 2-hop neighbourhood around the seeds\nsub = ego_graph(g, seeds; hops=2)\n\n# 3. Render into a token budget with compact prefixes\nctx = to_context(sub; budget=2000,\n                 prefixes=Dict(\"ex\" => \"http://example.org/\"))\n\nprompt = \"\"\"\nAnswer using only the facts below.\n\n$ctx\n\nQuestion: Who created Julia?\n\"\"\"\n\nego_graph deliberately does not expand through class nodes reached via rdf:type — classes are schema-level hubs, and expanding through them would pull every co-typed instance into the context (pass through_types=true if you want that).  Literals and predicates are never expanded through.\n\nWhen the rendered graph exceeds the budget, to_context keeps whole subject blocks greedily, most-connected subjects first, and guarantees the output stays within 4 × budget bytes (≈ tokens at 4 bytes/token).","title":"The pipeline"},{"category":"section","location":"ai/#Entity-profiles-—-Concise-Bounded-Description","page":"AI & GraphRAG","text":"cbd answers \"tell me everything about X\": all triples with subject X, recursing through blank-node values (addresses, structured values), and — by default — including the RDF-star annotations of every included fact, so confidence and provenance travel with the profile:\n\nprofile = cbd(g, ex.alice)               # facts + their annotations\nprintln(to_context(profile))","title":"Entity profiles — Concise Bounded Description"},{"category":"section","location":"ai/#Schema-introspection-for-text-to-SPARQL","page":"AI & GraphRAG","text":"LLMs write good SPARQL when handed the schema. describe_schema introspects a graph's actual shape from the data — no ontology required, which matters for the partially-typed graphs that extraction produces — and to_prompt renders it as compact, token-budgeted text:\n\nusing RDF\n\ns = describe_schema(g)            # classes, predicates, domains/ranges, examples\n\nschema_text = to_prompt(s; budget=1500,\n    prefixes=Dict(\"ex\" => \"http://example.org/\",\n                  \"foaf\" => \"http://xmlns.com/foaf/0.1/\"))\n\nprompt = \"\"\"\nYou are a SPARQL expert. Given this schema, write a query for the question.\n\n$schema_text\n\nQuestion: How old is the oldest person who works at Acme?\n\"\"\"\n\n# The model returns a query; validate it before running (ParseError on failure),\n# and feed the error message back for self-correction if it fails.\nquery = call_your_llm(prompt)\ntry\n    result = sparql(g, query)\ncatch e\n    e isa ParseError && (query = call_your_llm(prompt * \"\\n\\nThat query failed: $e\"))\nend\n\nto_prompt output looks like:\n\n# Classes\nfoaf:Person (3) — Person\nex:Organization (1)\n\n# Properties\nfoaf:name (3): foaf:Person → xsd:string  e.g. \"Alice\", \"Bob\"\nex:age (3): foaf:Person → xsd:integer  [age in years]  e.g. 30, 25\nfoaf:knows (2, multi): foaf:Person → foaf:Person  e.g. ex:p2, ex:p3\nex:worksAt (1): foaf:Person → ex:Organization  e.g. ex:acme\n\nEach property line shows its usage count, whether it is multi-valued, its domain (subject classes) → range (object classes and/or literal datatypes), any rdfs:label, and example values — everything a model needs to author a correct query.","title":"Schema introspection for text-to-SPARQL"},{"category":"function","location":"ai/#RDF.annotate!","page":"AI & GraphRAG","text":"annotate!(g, t::Triple, pred::IRI, obj) -> reifier\nannotate!(g, t::Triple; kwargs...)      -> reifier\n\nAssert triple t in g (if not already present) and attach an annotation to it using the RDF 1.2 reification pattern.  Returns the reifier node so further metadata can be attached to it directly.\n\nNon-RDFTerm annotation values are converted with Literal(); pass an IRI explicitly to link to a resource.  Repeated calls on the same triple reuse the same reifier, so all annotations describe one statement occurrence.\n\nIn the keyword form, each keyword k becomes the predicate anno.k in the exported anno namespace:\n\n# Store an LLM-extracted fact with its extraction metadata\nt = Triple(ex.alice, foaf.knows, ex.bob)\nannotate!(g, t; confidence=0.92, source=ex.doc7, model=Literal(\"claude-fable-5\"))\n\n# Read it back, filter by confidence\nfor v in annotations(g, t, anno.confidence)\n    value(Float64, v) > 0.9 && println(\"high-confidence fact\")\nend\n\nSee also annotations.\n\n\n\n\n\n","title":"RDF.annotate!"},{"category":"function","location":"ai/#RDF.annotations","page":"AI & GraphRAG","text":"annotations(g, t::Triple)            -> Vector{Pair{IRI, ObjectTerm}}\nannotations(g, t::Triple, pred::IRI) -> Vector{ObjectTerm}\n\nRetrieve the RDF 1.2 annotations attached to triple t (across all of its reifiers), as predicate => object pairs — or just the values for a single annotation predicate in the three-argument form.  The rdf:reifies link itself is not reported.  Returns an empty vector when t has no annotations.\n\nannotations(g, t)                   # [anno.confidence => Literal(0.92), …]\nannotations(g, t, anno.confidence)  # [Literal(0.92)]\n\n\n\n\n\n","title":"RDF.annotations"},{"category":"constant","location":"ai/#RDF.anno","page":"AI & GraphRAG","text":"anno\n\nThe RDF.jl annotation namespace (https://mthelm85.github.io/RDF.jl/vocab/annotation#).  Keyword arguments to annotate! become predicates in this namespace: confidence=0.9 attaches anno.confidence, source=doc attaches anno.source, and so on. Use it directly to query annotations back: annotations(g, t, anno.confidence).\n\n\n\n\n\n","title":"RDF.anno"},{"category":"function","location":"ai/#RDF.cbd","page":"AI & GraphRAG","text":"cbd(g, node; include_annotations=true) -> Graph\n\nThe Concise Bounded Description of node: every triple whose subject is node, plus — recursively — the CBD of every blank node that appears in object position of an included triple.  This is the standard \"everything this graph says about X\" subgraph.\n\nWhen include_annotations=true (the default), the RDF 1.2 annotations of every included triple (its reifiers and their properties — confidence scores, provenance, …) are included as well, so extraction metadata travels with the facts.\n\nprofile = cbd(g, ex.alice)\nprintln(to_context(profile))\n\nSee also ego_graph, to_context.\n\n\n\n\n\n","title":"RDF.cbd"},{"category":"function","location":"ai/#RDF.ego_graph","page":"AI & GraphRAG","text":"ego_graph(g, seeds; hops=1, through_types=false) -> Graph\n\nThe hops-hop neighbourhood subgraph around seeds (a single term or a collection): all triples incident — as subject or object — to the seed nodes, expanded hop by hop through IRI and blank-node neighbours.  Literals and predicates are never expanded through.\n\nClass nodes reached as the object of rdf:type are included but not expanded through by default — classes are schema-level hubs, and expanding through them would pull in every co-typed instance in the graph.  Pass through_types=true to expand through them anyway.\n\nThis is the standard retrieval step in GraphRAG pipelines: resolve a user query to seed entities, expand, then render with to_context:\n\nsub = ego_graph(g, [ex.julia, ex.bezanson]; hops=2)\nprompt_context = to_context(sub; budget=2000)\n\n\n\n\n\n","title":"RDF.ego_graph"},{"category":"function","location":"ai/#RDF.to_context","page":"AI & GraphRAG","text":"to_context(g; budget=nothing, prefixes=Dict()) -> String\n\nRender g as compact, subject-grouped Turtle text suitable for an LLM context window.\n\nbudget is an approximate token budget (estimated at 4 UTF-8 bytes per token).  When set, whole subject blocks are included greedily — most-connected subjects first, where a subject's score is its number of outgoing triples plus the number of triples that reference it — and the rendered output is guaranteed not to exceed 4 × budget bytes.  Without a budget the entire graph is rendered.\n\nprefixes (Dict{String,String} of prefix → IRI base) substantially compacts the output and is strongly recommended for token efficiency:\n\nctx = to_context(sub; budget=2000,\n                 prefixes=Dict(\"ex\" => \"http://example.org/\",\n                               \"foaf\" => \"http://xmlns.com/foaf/0.1/\"))\nprompt = \"Answer using only this knowledge:\\n$ctx\\n\\nQuestion: …\"\n\nSee also cbd, ego_graph.\n\n\n\n\n\n","title":"RDF.to_context"},{"category":"function","location":"ai/#RDF.describe_schema","page":"AI & GraphRAG","text":"describe_schema(g::Graph; max_examples=3) -> SchemaSummary\n\nIntrospect the structure of g from its data: the classes (objects of rdf:type) and predicates present, each predicate's domain (subject classes), range (literal datatypes and/or object classes), cardinality, up to max_examples example object values, and any rdfs:label / rdfs:comment.\n\ns = describe_schema(g)\nprompt = \"Schema:\\n\" * to_prompt(s; prefixes=Dict(\"ex\" => \"http://example.org/\")) *\n         \"\\n\\nWrite a SPARQL query that …\"\n\nSee also to_prompt.\n\n\n\n\n\n","title":"RDF.describe_schema"},{"category":"function","location":"ai/#RDF.to_prompt","page":"AI & GraphRAG","text":"to_prompt(s::SchemaSummary; budget=nothing, prefixes=Dict()) -> String\n\nRender a SchemaSummary as compact, deterministic text for an LLM system prompt: a # Classes section (name, instance count, label) and a # Properties section (name, count, domain → range, cardinality, examples, label).\n\nprefixes (prefix → IRI base) compacts IRIs to prefix:local and is strongly recommended. budget is an approximate token budget (≈ 4 bytes/token); when set, the most-used classes and predicates are kept first and the output is guaranteed not to exceed 4 × budget bytes.\n\nto_prompt(describe_schema(g); budget=1500,\n          prefixes=Dict(\"ex\" => \"http://example.org/\",\n                        \"foaf\" => \"http://xmlns.com/foaf/0.1/\"))\n\n\n\n\n\n","title":"RDF.to_prompt"},{"category":"type","location":"ai/#RDF.SchemaSummary","page":"AI & GraphRAG","text":"SchemaSummary\n\nThe result of describe_schema: classes and predicates (each sorted by usage, most frequent first) plus the total ntriples.  Render it for a prompt with to_prompt.\n\n\n\n\n\n","title":"RDF.SchemaSummary"},{"category":"type","location":"ai/#RDF.ClassInfo","page":"AI & GraphRAG","text":"ClassInfo\n\nA class discovered in a graph (an object of rdf:type): its iri, the number of distinct instances (count), and rdfs:label / rdfs:comment when present.\n\n\n\n\n\n","title":"RDF.ClassInfo"},{"category":"type","location":"ai/#RDF.PredicateInfo","page":"AI & GraphRAG","text":"PredicateInfo\n\nA predicate discovered in a graph, with the shape information an LLM needs to use it in a query:\n\niri, count — the predicate and how many triples use it\nsubject_classes — rdf:types of its subjects (domain), most frequent first\ndatatypes — datatypes of its literal objects (range, for data properties)\nobject_classes — rdf:types of its IRI/blank-node objects (range, for object properties), most frequent first\nexamples — a few representative object values\nmax_per_subject — the largest number of distinct values any single subject has (1 ⇒ effectively single-valued; >1 ⇒ multi-valued)\nlabel, comment — rdfs:label / rdfs:comment of the predicate, if present\n\n\n\n\n\n","title":"RDF.PredicateInfo"},{"category":"section","location":"querying/#Querying","page":"Querying","text":"","title":"Querying"},{"category":"section","location":"querying/#Pattern-matching","page":"Querying","text":"match, subjects, predicates, and objects are documented in the Graphs & Datasets page.\n\nmatch returns a lazy iterator of Triple (for a Graph) or Quad (for a Dataset) values. Any combination of the subject, predicate, and object keyword arguments can be provided; omitting one means \"wildcard\".\n\n# All triples\nfor t in match(g)\n    println(t)\nend\n\n# By predicate — finds all type assertions\nfor t in match(g; predicate=rdf.type)\n    println(t.subject, \" is a \", t.object)\nend\n\n# By subject — all properties of alice\nfor t in match(g; subject=ex.alice)\n    println(t.predicate, \" => \", t.object)\nend\n\n# By object — who has name \"Alice\"?\nfor t in match(g; predicate=foaf.name, object=Literal(\"Alice\"))\n    println(t.subject)\nend\n\nMatch results implement the Tables.jl interface:\n\nusing DataFrames\ndf = DataFrame(match(g; predicate=rdf.type))\n\n","title":"Pattern matching"},{"category":"section","location":"querying/#SPARQL-1.1-and-1.2","page":"Querying","text":"SPARQL is the W3C standard query language for RDF. RDF.jl implements the full SPARQL 1.1 query and update specification with 100% W3C test suite conformance (657/657 tests passing), plus SPARQL 1.2 / RDF-star — triple terms (<<( s p o )>>), reified triples (<< s p o >>), reifiers (~), annotation blocks ({| … |}), the triple-term builtins (TRIPLE, isTRIPLE, SUBJECT, PREDICATE, OBJECT), and directional language tags (LANGDIR/hasLANG/hasLANGDIR/STRLANGDIR) — passing 263/263 of the applicable W3C SPARQL 1.2 tests (the remainder require TriG input, which is not yet supported).","title":"SPARQL 1.1 and 1.2"},{"category":"section","location":"querying/#SELECT","page":"Querying","text":"Returns a SolutionSet — a columnar store of solution mappings from variable names (Symbol) to RDFTerm values (nothing for unbound / OPTIONAL variables).\n\nIterating a SolutionSet yields SolutionRow views; individual rows are accessible by integer index (result[i]).\n\nresult = sparql(ds, \"\"\"\n  PREFIX foaf: <http://xmlns.com/foaf/0.1/>\n  SELECT ?name ?age WHERE {\n    ?person foaf:name ?name ;\n            foaf:age  ?age .\n    FILTER(?age > 26)\n  } ORDER BY ?name\n\"\"\")\n\n# Iterate rows\nfor row in result\n    println(row[:name], \" is \", row[:age])\nend\n\n# Integer indexing\nfirst_row = result[1]\nfirst_row[:name]              # => Literal(\"Alice\")\n\n# get() with a default (safe for OPTIONAL variables)\nage = get(row, :age, nothing)\n\n# Coerce literal values with value()\nvalue(Int64, row[:age])       # => 30\n\n# With DataFrames (unbound variables become `missing`)\nusing DataFrames\ndf = DataFrame(result)","title":"SELECT"},{"category":"section","location":"querying/#ASK","page":"Querying","text":"Returns a Bool.\n\nsparql(ds, \"\"\"\n  PREFIX foaf: <http://xmlns.com/foaf/0.1/>\n  ASK { ?s foaf:age 30 }\n\"\"\")   # => true or false","title":"ASK"},{"category":"section","location":"querying/#CONSTRUCT","page":"Querying","text":"Returns a new Graph built from the CONSTRUCT template.\n\ng2 = sparql(ds, \"\"\"\n  PREFIX foaf: <http://xmlns.com/foaf/0.1/>\n  CONSTRUCT { ?s foaf:name ?n } WHERE { ?s foaf:name ?n }\n\"\"\")","title":"CONSTRUCT"},{"category":"section","location":"querying/#DESCRIBE","page":"Querying","text":"Returns a Graph describing the requested resources.\n\ng_desc = sparql(ds, \"DESCRIBE <http://example.org/alice>\")","title":"DESCRIBE"},{"category":"section","location":"querying/#Aggregates,-subqueries,-and-property-paths","page":"Querying","text":"# GROUP BY / COUNT\nsparql(ds, \"\"\"\n  PREFIX foaf: <http://xmlns.com/foaf/0.1/>\n  SELECT ?age (COUNT(?person) AS ?n) WHERE {\n    ?person foaf:age ?age\n  } GROUP BY ?age ORDER BY DESC(?n)\n\"\"\")\n\n# Property path: transitive foaf:knows closure\nsparql(ds, \"\"\"\n  PREFIX ex:   <http://example.org/>\n  PREFIX foaf: <http://xmlns.com/foaf/0.1/>\n  SELECT ?friend WHERE { ex:alice foaf:knows+ ?friend }\n\"\"\")\n\n# Subquery with LIMIT\nsparql(ds, \"\"\"\n  PREFIX foaf: <http://xmlns.com/foaf/0.1/>\n  SELECT ?name WHERE {\n    { SELECT ?person WHERE { ?person a foaf:Person } LIMIT 5 }\n    ?person foaf:name ?name\n  }\n\"\"\")","title":"Aggregates, subqueries, and property paths"},{"category":"section","location":"querying/#SPARQL-UPDATE","page":"Querying","text":"# INSERT DATA\nsparql_update!(ds, \"\"\"\n  PREFIX ex:   <http://example.org/>\n  PREFIX foaf: <http://xmlns.com/foaf/0.1/>\n  INSERT DATA { ex:carol foaf:name \"Carol\" ; foaf:age 28 }\n\"\"\")\n\n# DELETE / INSERT with WHERE\nsparql_update!(ds, \"\"\"\n  PREFIX foaf: <http://xmlns.com/foaf/0.1/>\n  DELETE { ?s foaf:age ?old }\n  INSERT { ?s foaf:age ?new }\n  WHERE  { ?s foaf:name \"Alice\" ; foaf:age ?old . BIND(?old + 1 AS ?new) }\n\"\"\")\n\n# CLEAR a named graph\nsparql_update!(ds, \"CLEAR GRAPH <http://example.org/g1>\")\n\n","title":"SPARQL UPDATE"},{"category":"section","location":"querying/#Remote-SPARQL-endpoints","page":"Querying","text":"Load HTTP.jl alongside RDF.jl and the same sparql function works against any remote SPARQL 1.1 endpoint — Wikidata, UniProt, DBpedia, your own triplestore, or anything else that speaks the W3C SPARQL Protocol.\n\nusing RDF, HTTP\n\n# Query Wikidata for chemical elements\nresults = sparql(\"https://query.wikidata.org/sparql\", \"\"\"\n  PREFIX wd:   <http://www.wikidata.org/entity/>\n  PREFIX wdt:  <http://www.wikidata.org/prop/direct/>\n  PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>\n  SELECT ?element ?name WHERE {\n    ?element wdt:P31 wd:Q11344 ;\n             rdfs:label ?name .\n    FILTER(LANG(?name) = \"en\")\n  } ORDER BY ?name LIMIT 20\n\"\"\")\n\nfor row in results\n    println(value(String, row[:name]))\nend\n\n# Into a DataFrame\nusing DataFrames\ndf = DataFrame(results)\n\nThe return types are identical to local queries:\n\nQuery form Return type\nSELECT SolutionSet\nASK Bool\nCONSTRUCT Graph\nDESCRIBE Graph","title":"Remote SPARQL endpoints"},{"category":"section","location":"querying/#Keyword-options","page":"Querying","text":"Keyword Default Description\ntimeout 60 Read timeout in seconds\nauth nothing (\"user\",\"pass\") for Basic auth; \"token\" for Bearer\nprefixes Dict() Extra PREFIX k => v declarations prepended to the query\ndefault_graph nothing default-graph-uri SPARQL Protocol parameter\nnamed_graph nothing named-graph-uri SPARQL Protocol parameter\nmethod :auto :get, :post, or :auto (GET ≤ 2 kB, POST otherwise)\nretries 2 Retry attempts on transient 5xx errors","title":"Keyword options"},{"category":"section","location":"querying/#Common-endpoint-URLs","page":"Querying","text":"Dataset Endpoint\nWikidata https://query.wikidata.org/sparql\nDBpedia https://dbpedia.org/sparql\nUniProt https://sparql.uniprot.org/sparql\nChEMBL https://chembl.bio2rdf.org/sparql\nLinked Data for Life Sciences https://lod.openlinksw.com/sparql","title":"Common endpoint URLs"},{"category":"section","location":"querying/#Extra-prefix-shortcuts","page":"Querying","text":"# Avoid repeating PREFIX declarations in every query\nresults = sparql(\"https://query.wikidata.org/sparql\",\n    \"SELECT ?item WHERE { ?item wdt:P31 wd:Q11344 } LIMIT 10\";\n    prefixes = Dict(\n        \"wd\"  => \"http://www.wikidata.org/entity/\",\n        \"wdt\" => \"http://www.wikidata.org/prop/direct/\"))","title":"Extra prefix shortcuts"},{"category":"section","location":"querying/#Authentication","page":"Querying","text":"# Basic auth\nsparql(\"https://my-endpoint.example/sparql\", query;\n       auth = (\"username\", \"password\"))\n\n# Bearer token\nsparql(\"https://my-endpoint.example/sparql\", query;\n       auth = \"eyJhbGci...\")","title":"Authentication"},{"category":"section","location":"querying/#Federated-queries-—-SERVICE","page":"Querying","text":"With HTTP.jl loaded, the standard SPARQL 1.1 SERVICE clause works inside local queries: the inner pattern is sent to the remote endpoint and the remote solutions are joined with the local ones on their shared variables.\n\nusing RDF, HTTP\n\n# Join local data with Wikidata\nresult = sparql(local_graph, \"\"\"\n  PREFIX wdt: <http://www.wikidata.org/prop/direct/>\n  PREFIX ex:  <http://example.org/>\n  SELECT ?city ?population WHERE {\n    ?city ex:officeLocation true .              # local\n    SERVICE <https://query.wikidata.org/sparql> {\n      ?city wdt:P1082 ?population               # remote\n    }\n  }\n\"\"\")\n\nSERVICE SILENT tolerates failures: if the endpoint is unreachable (or HTTP.jl is not loaded), the outer solutions pass through unchanged with the service variables left unbound. Without SILENT, failures raise an error — never silently empty results.","title":"Federated queries — SERVICE"},{"category":"section","location":"querying/#RemoteGraph-—-endpoint-backed-graph-view","page":"Querying","text":"RemoteGraph wraps an endpoint in the familiar Graph API, translating operations into SPARQL Protocol requests so the data never has to fit in local memory:\n\nusing RDF, HTTP\n\nwd = RemoteGraph(\"https://query.wikidata.org/sparql\")\n\ndouglas_adams = IRI(\"http://www.wikidata.org/entity/Q42\")\ninstance_of   = IRI(\"http://www.wikidata.org/prop/direct/P31\")\n\nmatch(wd; subject=douglas_adams, predicate=instance_of)  # SELECT under the hood\nTriple(douglas_adams, instance_of, IRI(\"http://www.wikidata.org/entity/Q5\")) in wd  # ASK\nsparql(wd, \"SELECT ?s WHERE { ?s ?p ?o } LIMIT 5\")       # direct forwarding\n\nRemoteGraph is read-only; supported operations are match, length, isempty, in, iteration, and sparql. Keyword arguments given to the constructor (auth, headers, timeout, …) are forwarded with every request.\n\n","title":"RemoteGraph — endpoint-backed graph view"},{"category":"section","location":"querying/#SPARQL-result-format-serialization","page":"Querying","text":"SolutionSet can be serialized to any of the four W3C standard result formats.","title":"SPARQL result format serialization"},{"category":"section","location":"querying/#SPARQL/JSON","page":"Querying","text":"io = IOBuffer()\nwrite(io, MIME\"application/sparql-results+json\"(), result)   # SELECT\nwrite(io, MIME\"application/sparql-results+json\"(), true)     # ASK\n\nOutput follows the W3C SPARQL 1.1 JSON format.","title":"SPARQL/JSON"},{"category":"section","location":"querying/#SPARQL/XML","page":"Querying","text":"write(io, MIME\"application/sparql-results+xml\"(), result)\nwrite(io, MIME\"application/sparql-results+xml\"(), false)\n\nOutput follows the W3C SPARQL 1.1 XML format.","title":"SPARQL/XML"},{"category":"section","location":"querying/#SPARQL/CSV","page":"Querying","text":"write(io, MIME\"text/csv\"(), result)\n\nFields containing commas or double-quotes are quoted per RFC 4180. Unbound variables produce empty fields.","title":"SPARQL/CSV"},{"category":"section","location":"querying/#SPARQL/TSV","page":"Querying","text":"write(io, MIME\"text/tab-separated-values\"(), result)\n\nHeader uses ?-prefixed variable names; term values use N-Triples syntax (<iri>, _:bnode, \"literal\"^^<dt>).\n\n","title":"SPARQL/TSV"},{"category":"section","location":"querying/#Supported-SPARQL-1.1-/-1.2-features","page":"Querying","text":"Feature Supported\nSELECT, CONSTRUCT, ASK, DESCRIBE ✅\nOPTIONAL, UNION, MINUS ✅\nFILTER (all functions and operators) ✅\nBIND, VALUES ✅\nGROUP BY, HAVING, aggregates ✅\nORDER BY, LIMIT, OFFSET, DISTINCT, REDUCED ✅\nSubqueries ✅\nProperty paths (/, ` ,*,+,?,^,!`)\nEXISTS / NOT EXISTS ✅\nGRAPH, FROM, FROM NAMED ✅\nINSERT DATA, DELETE DATA ✅\nINSERT/DELETE with WHERE ✅\nLOAD, CLEAR, DROP, COPY, MOVE, ADD ✅\nBuilt-in functions (str, lang, datatype, isIRI, isLiteral, …) ✅\nNumeric, string, date/time functions ✅\nHash functions (MD5, SHA1, SHA256, SHA384, SHA512) ✅\nSERVICE / SERVICE SILENT (federated queries) ✅ requires HTTP.jl\nSPARQL 1.2: triple terms <<( s p o )>> (patterns, VALUES, expressions) ✅\nSPARQL 1.2: reified triples << s p o >>, reifiers ~, annotation blocks {| … |} ✅\nSPARQL 1.2: TRIPLE, isTRIPLE, SUBJECT, PREDICATE, OBJECT ✅\nSPARQL 1.2: directional language tags, LANGDIR, hasLANG, hasLANGDIR, STRLANGDIR ✅\nSPARQL 1.2: VERSION declaration ✅","title":"Supported SPARQL 1.1 / 1.2 features"},{"category":"section","location":"querying/#Query-optimization","page":"Querying","text":"The SPARQL engine reorders the triple patterns inside each basic graph pattern before evaluation, so the order you write patterns in does not affect performance (or results). The optimizer uses exact cardinalities — the hexastore answers \"how many triples match this pattern?\" in O(log n) with two binary searches — and greedily evaluates the most selective pattern first, preferring patterns that share a variable with the already-bound set so that Cartesian products are avoided. No statistics, configuration, or query hints are needed.","title":"Query optimization"},{"category":"function","location":"querying/#RDF.sparql","page":"Querying","text":"sparql(g, query; base=nothing) → SolutionSet | Graph | Bool\n\nExecute a SPARQL 1.1 query against a Graph or Dataset.\n\nQuery form Return type\nSELECT SolutionSet\nASK Bool\nCONSTRUCT Graph\nDESCRIBE Graph\n\nbase is an optional absolute IRI used to resolve relative IRIs in the query (e.g. FROM <relative.ttl>). When nothing, relative IRIs in the query that lack a BASE declaration are resolved against an implementation-defined default.\n\nThrows ParseError on a syntactically invalid query, and RDFError on evaluation errors (e.g. an undeclared prefix).\n\n\n\n\n\nsparql(rg::RemoteGraph, query; kwargs...) → SolutionSet | Bool | Graph\n\nExecute a SPARQL query against the endpoint behind rg.  Keyword arguments are merged with (and override) those stored in the RemoteGraph.\n\n\n\n\n\n","title":"RDF.sparql"},{"category":"function","location":"querying/#RDF.sparql_parse","page":"Querying","text":"sparql_parse(src::AbstractString) → SpUnit\n\nParse a SPARQL 1.1 query or update string and return the AST. Returns SpQueryUnit for queries and SpUpdateUnit for updates. Throws ParseError on syntax errors.\n\n\n\n\n\n","title":"RDF.sparql_parse"},{"category":"type","location":"querying/#RDF.SolutionSet","page":"Querying","text":"SolutionSet\n\nThe result of a SPARQL SELECT query: an ordered sequence of solution mappings stored in a column-oriented layout.\n\nEach row maps a projected variable name (Symbol) to a bound RDFTerm, or nothing if the variable is unbound in that solution (common with OPTIONAL).\n\nIteration yields SolutionRow views; individual rows can be accessed by integer index (sol[i]).  Variable values are retrieved with row[:name].\n\nThe Tables.jl interface (via the RDFTablesExt extension) presents unbound variables as missing so that DataFrame(sol) works naturally.\n\nFor CONSTRUCT / DESCRIBE queries the return type is Graph; for ASK it is Bool.\n\n\n\n\n\n","title":"RDF.SolutionSet"},{"category":"type","location":"querying/#RDF.SolutionRow","page":"Querying","text":"SolutionRow\n\nA lightweight view of a single row in a SolutionSet.\n\nSupports:\n\nrow[:varname]                     — retrieve a binding (RDFTerm or nothing)\nget(row, :varname, default)       — retrieve with a default\nkeys(row)                         — all variable names in the parent SolutionSet\n\n\n\n\n\n","title":"RDF.SolutionRow"},{"category":"function","location":"querying/#RDF.read_sparql_json","page":"Querying","text":"read_sparql_json(body::AbstractString) → SolutionSet | Bool\n\nParse a W3C SPARQL 1.1 JSON result document (the body of an application/sparql-results+json HTTP response) into a SolutionSet for SELECT results or a Bool for ASK results.\n\nThis is a low-level helper primarily used by the RDFHTTPExt extension for consuming remote SPARQL endpoint responses.  Normal users call sparql(url, query) after loading HTTP.jl.\n\n\n\n\n\n","title":"RDF.read_sparql_json"},{"category":"function","location":"querying/#RDF.sparql_update!","page":"Querying","text":"sparql_update!(ds, update; base=nothing)\n\nExecute a SPARQL 1.1 Update operation against a Dataset, mutating it in place. Multiple update operations separated by ; are supported.\n\nbase is an optional absolute IRI used to resolve relative IRIs in the update.\n\nThrows ParseError on a syntactically invalid update.\n\n\n\n\n\n","title":"RDF.sparql_update!"},{"category":"type","location":"querying/#RDF.RemoteGraph","page":"Querying","text":"RemoteGraph(endpoint::AbstractString; kwargs...)\n\nA read-only, Graph-like view of a remote SPARQL endpoint.  Pattern matching, counting, membership tests, and iteration are translated into SPARQL Protocol requests, so data never has to fit in local memory:\n\nusing RDF, HTTP\n\nwikidata = RemoteGraph(\"https://query.wikidata.org/sparql\")\n\n# Pattern match → SELECT query\nfor t in match(wikidata; subject=IRI(\"http://www.wikidata.org/entity/Q42\"),\n                          predicate=IRI(\"http://www.wikidata.org/prop/direct/P31\"))\n    println(t.object)\nend\n\n# Membership test → ASK query\nTriple(s, p, o) in wikidata\n\n# Forward a full SPARQL query\nsparql(wikidata, \"SELECT ?s WHERE { ?s ?p ?o } LIMIT 10\")\n\nKeyword arguments (auth, headers, timeout, …) are stored and forwarded to every HTTP request — see the remote sparql documentation for the full list.  Requires HTTP.jl (using HTTP).\n\nSupported operations: match, length, isempty, in, iteration (collect), and sparql.  Mutation (push!, delete!) is not supported; use sparql_update! against an update endpoint instead.\n\n\n\n\n\n","title":"RDF.RemoteGraph"},{"category":"type","location":"querying/#RDF.RemoteEndpointError","page":"Querying","text":"RemoteEndpointError <: RDFError\n\nA remote SPARQL endpoint operation failed: the HTTP transport is unavailable, the endpoint returned an error status or an unparseable response, or a federated SERVICE clause could not be evaluated.  endpoint is the endpoint URL (or a placeholder such as \"(variable)\" when no concrete URL exists).\n\n\n\n\n\n","title":"RDF.RemoteEndpointError"},{"category":"section","location":"terms/#RDF-Terms","page":"Terms","text":"RDF data is built from three kinds of terms. All three are subtypes of RDFTerm.","title":"RDF Terms"},{"category":"section","location":"terms/#Type-hierarchy","page":"Terms","text":"RDFTerm\n├── IRI\n├── BlankNode\n├── Literal\n└── TripleTerm   (RDF-star — embedded triples in subject or object position)","title":"Type hierarchy"},{"category":"section","location":"terms/#IRI","page":"Terms","text":"An absolute IRI (RFC 3987). IRIs identify resources and are used as subjects, predicates, and objects.","title":"IRI"},{"category":"section","location":"terms/#Construction","page":"Terms","text":"IRI(\"http://example.org/Alice\")    # runtime validation\niri\"http://schema.org/Person\"      # compile-time validation\n\nValidation rules: must begin with a scheme (http:, urn:, etc.) and must not contain unencoded spaces. IRIs are interned globally — two IRI values with the same string are ===.","title":"Construction"},{"category":"section","location":"terms/#BlankNode","page":"Terms","text":"A blank node represents an unnamed resource local to a graph. Blank nodes should be created via blank! to guarantee unique identifiers.\n\ng  = Graph()\nb1 = blank!(g)         # mint one blank node belonging to g\nb2, b3 = blank!(g, 2)  # mint two at once","title":"BlankNode"},{"category":"section","location":"terms/#Literal","page":"Terms","text":"A typed value with a lexical form, datatype IRI, and optional language tag.","title":"Literal"},{"category":"section","location":"terms/#Common-constructors","page":"Terms","text":"Expression Datatype\nLiteral(\"hello\") xsd:string\nLiteral(\"hello\"; lang=\"en\") rdf:langString\nLiteral(\"hello\"; lang=\"ar\", dir=\"rtl\") rdf:dirLangString (RDF 1.2)\nLiteral(\"hello\", IRI(\"...\")) explicit datatype\nLiteral(42) xsd:integer\nLiteral(3.14) xsd:double\nLiteral(true) xsd:boolean\nLiteral(Date(2024,1,1)) xsd:date\nLiteral(DateTime(2024,1,1,12,0,0)) xsd:dateTime","title":"Common constructors"},{"category":"section","location":"terms/#Coercing-a-literal-to-a-Julia-value","page":"Terms","text":"lit = Literal(42)\n\n# One-argument: infer Julia type from the RDF datatype\nvalue(lit)              # => 42  (Int64, because xsd:integer)\n\n# Two-argument: convert to a requested type\nvalue(Int64,   lit)     # => 42\nvalue(Float64, lit)     # => 42.0   (Int64 → Float64 via convert)\nvalue(Int64,   Literal(3.14))   # throws LiteralValueError (inexact)\n\n# Safe variant — returns nothing instead of throwing\ntryvalue(lit)                          # => 42\ntryvalue(Float64, lit)                 # => 42.0\ntryvalue(Int64,   Literal(3.14))       # => nothing\ntryvalue(Int64,   Literal(\"oops\", xsd.integer))  # => nothing\n\nThe natural Julia type for each RDF datatype:\n\nRDF datatype Julia type\nxsd:string, rdf:langString String\nxsd:boolean Bool\nxsd:integer and all integer subtypes Int64 (or BigInt if too large)\nxsd:double Float64\nxsd:float Float32\nxsd:decimal BigFloat (finite-precision approximation)\nxsd:date Dates.Date\nxsd:dateTime, xsd:dateTimeStamp Dates.DateTime (timezone stripped)\n\nThe two-argument value(T, lit) uses Julia's convert, so any numeric widening that convert supports works (e.g. Int64 → Float64, Float32 → Float64). Abstract supertypes like Number or AbstractFloat work too.","title":"Coercing a literal to a Julia value"},{"category":"section","location":"terms/#TripleTerm","page":"Terms","text":"TripleTerm (RDF-star) allows an embedded triple to appear in both subject and object positions, enabling statement-level annotations without blank-node reification.\n\ntt = TripleTerm(ex.alice, ex.age, Literal(30))\npush!(g, Triple(ex.claim, rdf.reifies, tt))","title":"TripleTerm"},{"category":"section","location":"terms/#Triple-and-Quad","page":"Terms","text":"A Triple has three fields: subject::SubjectTerm, predicate::IRI, object::ObjectTerm. See Triple in the Graphs & Datasets page for the full API.\n\nA Quad is a named-graph extension — (subject, predicate, object, graph) — used when iterating Dataset quads.","title":"Triple and Quad"},{"category":"section","location":"terms/#Type-aliases","page":"Terms","text":"Alias Allowed types\nSubjectTerm IRI, BlankNode, TripleTerm\nPredicateTerm IRI\nObjectTerm IRI, BlankNode, Literal, TripleTerm\nGraphName IRI, BlankNode","title":"Type aliases"},{"category":"section","location":"terms/#Namespace-helper","page":"Terms","text":"Namespace provides a convenient shorthand for constructing IRIs within a common prefix.\n\nex   = Namespace(\"http://example.org/\")\nfoaf = Namespace(\"http://xmlns.com/foaf/0.1/\")\n\nex.alice            # => IRI(\"http://example.org/alice\")\nfoaf.name           # => IRI(\"http://xmlns.com/foaf/0.1/name\")\nex[\"with spaces\"]   # => IRI(\"http://example.org/with%20spaces\")","title":"Namespace helper"},{"category":"section","location":"terms/#Built-in-vocabulary-modules","page":"Terms","text":"Prefix modules expose every term from common namespaces as named constants:\n\nrdf.type          # IRI(\"http://www.w3.org/1999/02/22-rdf-syntax-ns#type\")\nrdfs.subClassOf   # IRI(\"http://www.w3.org/2000/01/rdf-schema#subClassOf\")\nxsd.integer       # IRI(\"http://www.w3.org/2001/XMLSchema#integer\")\nowl.sameAs        # IRI(\"http://www.w3.org/2002/07/owl#sameAs\")\nskos.prefLabel    # IRI(\"http://www.w3.org/2004/02/skos/core#prefLabel\")\ndc.title          # IRI(\"http://purl.org/dc/elements/1.1/title\")\ndcterms.creator   # IRI(\"http://purl.org/dc/terms/creator\")\nfoaf.name         # IRI(\"http://xmlns.com/foaf/0.1/name\")\nschema.Person     # IRI(\"https://schema.org/Person\")\n\nAvailable modules: rdf, rdfs, xsd, owl, skos, dc, dcterms, foaf, schema.","title":"Built-in vocabulary modules"},{"category":"type","location":"terms/#RDF.RDFTerm","page":"Terms","text":"RDFTerm\n\nAbstract supertype for all RDF terms: IRI, BlankNode, Literal, and TripleTerm.\n\n\n\n\n\n","title":"RDF.RDFTerm"},{"category":"type","location":"terms/#RDF.IRI","page":"Terms","text":"IRI(s::AbstractString) <: RDFTerm\n\nAn absolute IRI (RFC 3987). Must begin with a scheme (e.g. http:) and contain no unencoded spaces. Use the iri\"\" string macro for compile-time validation.\n\nIRI(\"http://example.org/Alice\")\niri\"http://schema.org/Person\"\n\n\n\n\n\n","title":"RDF.IRI"},{"category":"macro","location":"terms/#RDF.@iri_str","page":"Terms","text":"iri\"http://example.org/foo\"\n\nString macro that constructs an IRI and validates the value at compile time. Equivalent to IRI(s) but catches invalid IRIs before the program runs.\n\n\n\n\n\n","title":"RDF.@iri_str"},{"category":"type","location":"terms/#RDF.BlankNode","page":"Terms","text":"BlankNode <: RDFTerm\n\nAn RDF blank node identified by an opaque UInt64 id. Blank nodes should be created via blank! rather than constructed directly, to ensure ids are unique within the graph.\n\n\n\n\n\n","title":"RDF.BlankNode"},{"category":"function","location":"terms/#RDF.blank!","page":"Terms","text":"blank!(g::Graph) -> BlankNode\nblank!(g::Graph, n::Int) -> Vector{BlankNode}\n\nMint one or more fresh blank nodes registered to graph g.\n\n\n\n\n\n","title":"RDF.blank!"},{"category":"type","location":"terms/#RDF.Literal","page":"Terms","text":"Literal <: RDFTerm\n\nAn RDF literal with a lexical form, datatype IRI, and optional language tag.\n\nCommon constructors:\n\nLiteral(\"hello\") — plain string (xsd:string)\nLiteral(\"hello\"; lang=\"en\") — language-tagged string (rdf:langString)\nLiteral(\"hello\"; lang=\"ar\", dir=\"rtl\") — directional string (rdf:dirLangString, RDF 1.2)\nLiteral(\"hello\", IRI(\"...\")) — explicit datatype\nLiteral(42) → xsd:integer, Literal(3.14) → xsd:double, Literal(true) → xsd:boolean\nLiteral(Date(2024,1,1)) → xsd:date\n\n\n\n\n\n","title":"RDF.Literal"},{"category":"function","location":"terms/#RDF.value","page":"Terms","text":"value(lit::Literal)\nvalue(T::Type, lit::Literal)\n\nParse the lexical form of lit to a Julia value.\n\nThe one-argument form infers the target type from the datatype IRI:\n\nXSD / RDF datatype Julia type\nxsd:string, rdf:langString, rdf:dirLangString String\nxsd:boolean Bool\nxsd:integer and all integer subtypes Int64 (or BigInt if too large)\nxsd:double Float64\nxsd:float Float32\nxsd:decimal BigFloat (finite-precision approximation)\nxsd:date Dates.Date\nxsd:dateTime, xsd:dateTimeStamp Dates.DateTime (timezone stripped)\n\nThe two-argument form additionally converts the result to T using Julia's convert. This means numeric widening works naturally:\n\nvalue(Literal(42))              # => 42  (Int64)\nvalue(Int64,   Literal(42))     # => 42\nvalue(Float64, Literal(42))     # => 42.0   (Int64 → Float64 via convert)\nvalue(Int64,   Literal(3.14))   # throws LiteralValueError (inexact)\n\nThrows LiteralValueError if the lexical form cannot be parsed or if the resulting value cannot be converted to T.  See tryvalue for a non-throwing variant.\n\n\n\n\n\n","title":"RDF.value"},{"category":"function","location":"terms/#RDF.tryvalue","page":"Terms","text":"tryvalue(lit::Literal)\ntryvalue(T::Type, lit::Literal)\n\nLike value but returns nothing instead of throwing on failure.\n\ntryvalue(Literal(42))               # => 42\ntryvalue(Float64, Literal(42))      # => 42.0\ntryvalue(Int64,   Literal(3.14))    # => nothing  (inexact)\ntryvalue(Int64,   Literal(\"oops\", xsd.integer))  # => nothing\n\n\n\n\n\n","title":"RDF.tryvalue"},{"category":"type","location":"terms/#RDF.TripleTerm","page":"Terms","text":"TripleTerm(subject, predicate, object) <: RDFTerm\n\nAn RDF-star triple term — an embedded triple used as an RDF term in either subject or object position. Enables statement-level annotations (e.g. via rdf:reifies) without blank-node reification.\n\nThe subject may be an IRI, BlankNode, or nested TripleTerm; the predicate must be an IRI; the object may be any ObjectTerm.\n\ntt = TripleTerm(iri\"http://example.org/bob\", rdf.type, iri\"http://example.org/Person\")\n\n# As object (most common — rdf:reifies annotation)\npush!(g, Triple(iri\"http://example.org/claim\", rdf.reifies, tt))\n\n# As subject\npush!(g, Triple(tt, iri\"http://example.org/certainty\", Literal(0.9)))\n\n\n\n\n\n","title":"RDF.TripleTerm"},{"category":"type","location":"terms/#RDF.SubjectTerm","page":"Terms","text":"SubjectTerm\n\nUnion type for valid RDF subject positions: IRI, BlankNode, or TripleTerm (RDF-star embedded triple).\n\n\n\n\n\n","title":"RDF.SubjectTerm"},{"category":"type","location":"terms/#RDF.PredicateTerm","page":"Terms","text":"PredicateTerm\n\nAlias for IRI; only IRIs are valid in the predicate position.\n\n\n\n\n\n","title":"RDF.PredicateTerm"},{"category":"type","location":"terms/#RDF.ObjectTerm","page":"Terms","text":"ObjectTerm\n\nUnion type for valid RDF object positions: IRI, BlankNode, Literal, or TripleTerm.\n\n\n\n\n\n","title":"RDF.ObjectTerm"},{"category":"type","location":"terms/#RDF.GraphName","page":"Terms","text":"GraphName\n\nUnion type for RDF graph names: IRI or BlankNode.\n\n\n\n\n\n","title":"RDF.GraphName"},{"category":"type","location":"terms/#RDF.Namespace","page":"Terms","text":"Namespace(base::String)\n\nA namespace prefix for constructing IRIs using dot notation or bracket indexing.\n\nex  = Namespace(\"http://example.org/\")\nex.Alice          # => IRI(\"http://example.org/Alice\")\nex[\"first-name\"]  # => IRI(\"http://example.org/first-name\")\n\n\n\n\n\n","title":"RDF.Namespace"},{"category":"section","location":"#RDF.jl","page":"Home","text":"A full-featured RDF 1.1 library for Julia.\n\nGraphs are backed by a hexastore index — six sorted arrays covering every (s, p, o) permutation — giving O(log n) pattern matching on any combination of subject, predicate, and object. The SPARQL 1.1 engine is 100% W3C conformant (657/657 tests passing).","title":"RDF.jl"},{"category":"section","location":"#Features","page":"Home","text":"SPARQL 1.1 — SELECT, CONSTRUCT, ASK, DESCRIBE; full update language (INSERT, DELETE, LOAD, COPY, …); subqueries, aggregates, property paths, BIND, VALUES, EXISTS/NOT EXISTS, OPTIONAL, UNION, MINUS, GRAPH, FROM/FROM NAMED\nTurtle 1.1 — parser and serializer\nN-Triples / N-Quads — parser and serializer\nJSON-LD 1.1 — parser and serializer with inline context support\nSPARQL result formats — SPARQL/JSON, SPARQL/XML, CSV, TSV serialization\nNamed graphs / Datasets — full SPARQL dataset semantics\nRDFS inference — forward-chaining closure, entailment check\nGraph isomorphism — blank-node bijection\nTables.jl integration — match results and SolutionSet work directly with DataFrames and any Tables.jl consumer\nVocabulary API — load any external ontology or namespace as a Vocabulary with dot-notation term access (vocab.BachelorDegree), rdfs:label / rdfs:comment metadata, and HTTP loading via HTTP.jl\nBuilt-in vocabulary modules: rdf, rdfs, xsd, owl, skos, dc, dcterms, foaf, schema","title":"Features"},{"category":"section","location":"#Installation","page":"Home","text":"pkg> add RDF","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"","title":"Quick start"},{"category":"section","location":"#Building-a-graph","page":"Home","text":"using RDF\n\nex = Namespace(\"http://example.org/\")\n\ng = Graph()\npush!(g, Triple(ex.alice, rdf.type,  ex.Person))\npush!(g, Triple(ex.alice, foaf.name, Literal(\"Alice\")))\npush!(g, Triple(ex.alice, foaf.age,  Literal(30)))\npush!(g, Triple(ex.bob,   rdf.type,  ex.Person))\npush!(g, Triple(ex.bob,   foaf.name, Literal(\"Bob\")))\n\n# Pattern matching — any combination of subject/predicate/object\nfor t in match(g; predicate=rdf.type, object=ex.Person)\n    println(t.subject)\nend\n\n# Coerce a literal to a Julia value\nage_lit = first(match(g; subject=ex.alice, predicate=foaf.age)).object\nvalue(Int64, age_lit)  # => 30","title":"Building a graph"},{"category":"section","location":"#SPARQL-queries","page":"Home","text":"result = sparql(Dataset(; default_graph=g), \"\"\"\n  PREFIX foaf: <http://xmlns.com/foaf/0.1/>\n  SELECT ?name ?age WHERE {\n    ?person foaf:name ?name ;\n            foaf:age  ?age .\n    FILTER(?age > 26)\n  } ORDER BY ?name\n\"\"\")\n\nfor row in result\n    println(row[:name], \" — age \", row[:age])\nend\n# \"Alice\" — age 30","title":"SPARQL queries"},{"category":"section","location":"#Named-graphs-/-Datasets","page":"Home","text":"ds = Dataset()\nds[IRI(\"http://example.org/graph1\")] = g\n\nresult = sparql(ds, \"\"\"\n  SELECT ?g ?s WHERE { GRAPH ?g { ?s a <http://example.org/Person> } }\n\"\"\")","title":"Named graphs / Datasets"},{"category":"section","location":"#Serialization","page":"Home","text":"# Turtle (parse + write)\ng = read(\"data.ttl\", MIME\"text/turtle\"(), Graph)\nwrite(io, MIME\"text/turtle\"(), g)\n\n# JSON-LD (parse + write)\ng = read(\"data.jsonld\", MIME\"application/ld+json\"(), Graph)\nwrite(io, MIME\"application/ld+json\"(), g)\n\n# N-Triples / N-Quads\nwrite(io, MIME\"application/n-triples\"(), g)\nwrite(io, MIME\"application/n-quads\"(), ds)\n\n# Convenience: dispatch on file extension (.ttl / .nt / .nq / .jsonld)\nrdf_write(\"data.nt\", g)\ng = rdf_read(\"data.ttl\")","title":"Serialization"},{"category":"section","location":"#SPARQL-result-serialization","page":"Home","text":"result = sparql(ds, \"SELECT * WHERE { ?s ?p ?o }\")\n\n# SPARQL/JSON (for HTTP APIs)\nwrite(io, MIME\"application/sparql-results+json\"(), result)\n\n# SPARQL/XML\nwrite(io, MIME\"application/sparql-results+xml\"(), result)\n\n# CSV / TSV\nwrite(io, MIME\"text/csv\"(), result)\nwrite(io, MIME\"text/tab-separated-values\"(), result)","title":"SPARQL result serialization"},{"category":"section","location":"#RDFS-inference","page":"Home","text":"infer_rdfs(g)   # returns a new graph with the RDFS closure\ninfer_rdfs!(g)  # closes g in place\nentails(g, Triple(ex.alice, rdf.type, ex.Animal))","title":"RDFS inference"},{"category":"section","location":"#Tables.jl-integration","page":"Home","text":"Match results and SPARQL SolutionSets implement the Tables.jl interface with automatic coercion to native Julia types:\n\nRDF type Julia column type\nIRI String (full URI)\nBlankNode String (\"_:b{id}\")\nxsd:integer and sub-types Int64\nxsd:double / xsd:float / xsd:decimal Float64\nxsd:boolean Bool\nxsd:date Dates.Date\nxsd:dateTime Dates.DateTime\nxsd:string, rdf:langString, other literals String (lexical form)\nUnbound OPTIONAL variable missing\nHeterogeneous column String\n\nusing DataFrames\n\n# match → DataFrame\ndf = DataFrame(match(g; predicate=rdf.type))\n# subject, predicate, object columns are all String\n\n# SPARQL SELECT → DataFrame with typed columns\ndf = DataFrame(sparql(ds, \"\"\"\n  PREFIX foaf: <http://xmlns.com/foaf/0.1/>\n  SELECT ?name ?age WHERE { ?s foaf:name ?name ; foaf:age ?age }\n\"\"\"))\n# name::String, age::Int64 — ready for analysis","title":"Tables.jl integration"},{"category":"section","location":"#Vocabulary-API","page":"Home","text":"Load any external ontology or namespace and access its terms by name:\n\nusing RDF, HTTP\n\n# Load CTDL from Credential Engine (any Turtle / N-Triples / JSON-LD URL works)\nctdl = load_vocabulary(\"https://credreg.net/ctdl/schema/encoding/turtle\";\n                        base=\"http://purl.org/ctdl/terms/\")\n\nctdl.BachelorDegree              # => IRI(\"http://purl.org/ctdl/terms/BachelorDegree\")\nlabel(ctdl, ctdl.Course)         # => \"Course\"\ncomment(ctdl, ctdl.estimatedCost)\n\n# Use directly in a SPARQL query\nsparql(ds, \"\"\"\n  SELECT ?cred WHERE { ?cred a <$(ctdl.BachelorDegree)> . }\n\"\"\")\n\n# From a local file\ngo = load_vocabulary(\"go.ttl\"; base=\"http://purl.obolibrary.org/obo/\")\n\nSee the Vocabulary API page for the full reference.","title":"Vocabulary API"},{"category":"section","location":"#Built-in-vocabularies","page":"Home","text":"rdf.type          # IRI(\"http://www.w3.org/1999/02/22-rdf-syntax-ns#type\")\nrdfs.subClassOf   # IRI(\"http://www.w3.org/2000/01/rdf-schema#subClassOf\")\nxsd.integer       # IRI(\"http://www.w3.org/2001/XMLSchema#integer\")\nowl.sameAs        # IRI(\"http://www.w3.org/2002/07/owl#sameAs\")\nfoaf.name         # IRI(\"http://xmlns.com/foaf/0.1/name\")\nschema.Person     # IRI(\"https://schema.org/Person\")","title":"Built-in vocabularies"},{"category":"section","location":"inference/#Inference-and-Validation","page":"Inference & Validation","text":"","title":"Inference & Validation"},{"category":"section","location":"inference/#RDFS-Inference","page":"Inference & Validation","text":"RDF.jl implements RDFS forward-chaining materialization — computing the full deductive closure of a graph under the RDFS entailment rules.","title":"RDFS Inference"},{"category":"section","location":"inference/#Available-rules","page":"Inference & Validation","text":"Symbol RDFS rule Description\n:subClassOf rdfs9, rdfs10, rdfs11 Transitive class hierarchy + type propagation\n:subPropertyOf rdfs5, rdfs7 Transitive property hierarchy + triple rewriting\n:domain rdfs2 ?x a ?C when ?x ?p ?y and ?p rdfs:domain ?C\n:range rdfs3 ?y a ?C when ?x ?p ?y and ?p rdfs:range ?C\n\nAll rules are applied by default. Pass a subset via the rules keyword to restrict which rules fire:\n\n# Only subclass inference\nclosed = infer_rdfs(g; rules=[:subClassOf])\n\n# All RDFS rules (default)\nclosed = infer_rdfs(g)","title":"Available rules"},{"category":"section","location":"inference/#Example","page":"Inference & Validation","text":"using RDF\n\nex   = Namespace(\"http://example.org/\")\nrdfs = RDF.rdfs\n\ng = Graph()\npush!(g, Triple(ex.Cat,   rdfs.subClassOf, ex.Animal))\npush!(g, Triple(ex.Tiger, rdfs.subClassOf, ex.Cat))\npush!(g, Triple(ex.tigger, rdf.type,       ex.Tiger))\n\nclosed = infer_rdfs(g)\n\n# Now derivable:\nentails(closed, Triple(ex.tigger, rdf.type, ex.Cat))     # true via rdfs9\nentails(closed, Triple(ex.tigger, rdf.type, ex.Animal))  # true via rdfs11+rdfs9","title":"Example"},{"category":"section","location":"inference/#In-place-materialization","page":"Inference & Validation","text":"infer_rdfs! modifies the graph directly (no copy):\n\ninfer_rdfs!(g)  # adds all RDFS-entailed triples to g","title":"In-place materialization"},{"category":"section","location":"inference/#Entailment-check","page":"Inference & Validation","text":"entails checks whether a graph entails a specific triple, optionally under a named entailment regime:\n\nentails(g, Triple(ex.alice, rdf.type, ex.Animal))          # :rdfs (default)\nentails(g, Triple(ex.alice, rdf.type, ex.Animal); regime=:simple)  # direct membership only\n\n","title":"Entailment check"},{"category":"section","location":"inference/#Validation","page":"Inference & Validation","text":"validate checks every literal in a graph against its declared datatype. It returns a vector of ValidationWarning values describing any mismatches.\n\nwarnings = validate(g)\nfor w in warnings\n    println(w.message, \" at \", w.triple)\nend","title":"Validation"},{"category":"section","location":"inference/#Checked-datatypes","page":"Inference & Validation","text":"The validator checks lexical forms for the following XSD datatypes:\n\nNumeric: xsd:integer, xsd:long, xsd:int, xsd:short, xsd:byte, xsd:nonNegativeInteger, xsd:positiveInteger, xsd:nonPositiveInteger, xsd:negativeInteger, xsd:unsignedLong, xsd:unsignedInt, xsd:unsignedShort, xsd:unsignedByte, xsd:decimal, xsd:float, xsd:double\nBoolean: xsd:boolean\nDate/time: xsd:date, xsd:dateTime, xsd:dateTimeStamp\nLanguage tags: rdf:langString, rdf:dirLangString (RDF 1.2)\n\nA ValidationWarning is produced (not an exception) to allow partial processing of real-world data that may have conformance issues.","title":"Checked datatypes"},{"category":"function","location":"inference/#RDF.infer_rdfs","page":"Inference & Validation","text":"infer_rdfs(g::Graph; rules=[:subClassOf, :subPropertyOf, :domain, :range]) -> Graph\n\nReturn a new graph that is the RDFS closure of g — the original triples plus all triples derivable by the applicable RDFS entailment rules (forward-chaining to fixed point). g is not modified.\n\nAvailable rules: :subClassOf, :subPropertyOf, :domain, :range.\n\nclosed = infer_rdfs(g)\nentails(g, Triple(ex.alice, rdf.type, ex.Animal))  # checks via RDFS closure\n\n\n\n\n\n","title":"RDF.infer_rdfs"},{"category":"function","location":"inference/#RDF.infer_rdfs!","page":"Inference & Validation","text":"infer_rdfs!(g::Graph; rules=...) -> g\n\nIn-place version of infer_rdfs. Adds all RDFS-entailed triples directly to g and returns it.\n\n\n\n\n\n","title":"RDF.infer_rdfs!"},{"category":"function","location":"inference/#RDF.entails","page":"Inference & Validation","text":"entails(g::Graph, t::Triple; regime::Symbol=:rdfs) -> Bool\n\nReturn true if g entails t under the given entailment regime.\n\n:simple — checks direct membership (t ∈ g)\n:rdfs — checks membership in the RDFS closure of g\n\nentails(g, Triple(ex.alice, rdf.type, ex.Animal))\nentails(g, Triple(ex.alice, rdf.type, ex.Animal); regime=:simple)\n\n\n\n\n\n","title":"RDF.entails"},{"category":"function","location":"inference/#RDF.validate","page":"Inference & Validation","text":"validate(g::Graph) -> Vector{ValidationWarning}\nvalidate(ds::Dataset) -> Vector{ValidationWarning}\n\nCheck all literals in g (or ds) for conformance issues and return a list of ValidationWarnings. An empty list means no issues were found.\n\nChecks performed:\n\nLanguage tags must conform to BCP47 syntax\nLexical forms must be valid for known XSD datatypes\n\nwarns = validate(g)\nisempty(warns) || foreach(w -> @warn(w.message), warns)\n\n\n\n\n\n","title":"RDF.validate"},{"category":"type","location":"inference/#RDF.ValidationWarning","page":"Inference & Validation","text":"ValidationWarning\n\nA warning produced by validate, containing:\n\ntriple::Triple — the offending triple\nmessage::String — human-readable description\ncode::Symbol — machine-readable code (:invalid_lang_tag, :ill_typed_literal)\n\n\n\n\n\n","title":"RDF.ValidationWarning"}]
}
